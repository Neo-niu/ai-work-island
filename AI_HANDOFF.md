# AI 接手说明

## 目的

将 Codex 与本地自动化程序汇总为一个状态中枢，并通过桌面面板、悬浮胶囊和 Touch Bar 减少切窗口检查。

## 背景

项目基于 `MarlonJD/codex_touchbar`。用户本机为 M2 Touch Bar MacBook Pro，已安装 Codex/ChatGPT、Hermes Desktop 与 Hermes CLI。

## 当前实现

- 2026-08-28：修复完成态 Codex 任务卡绿色结果条视觉偏低。结果正文改用可选择的 `NSTextView` 后，绿条仍沿用运行态的 `2pt` 顶部下移；现单独将结果条顶部内缩设为 `0pt`，运行态蓝条和其他布局不变。新增对应布局回归；针对性测试、构建安装、严格签名、单实例、Hermes/自动化诊断和 `git diff --check` 通过。安装版已展开真实完成卡截图回读，绿条与结果首行对齐。全量发布脚本中既有 `behindWindow` 普通胶囊快照仍因桌面取样产生非确定像素差异，本次未接受或重录无关基线。

- 2026-08-28：重做任务完成更新胶囊的排版与动效。完成态由 `260×56pt` 调整为 `288×64pt`，状态点改为整块内容垂直居中，标题/摘要采用 `13.5pt semibold / 11.5pt regular` 两级字阶与更宽左右留白，连续圆角为 22pt。外壳仍从原胶囊锚点连续扩展；标题与摘要在 50ms 后以 2–5pt 上移和强 ease-out 分层显现，完整内容过渡 280ms，无弹跳，并在“减少动态效果”下取消位移。新增动画键、尺寸与完成态视觉回归；242 项非快照测试和完成态独立视觉快照通过。当前工作树既有 `behindWindow` 普通胶囊快照会随桌面取样产生非确定像素差异，未重录无关 20 张基线。新版已安装到 `/Applications/AI工作岛.app`；严格签名、单实例、Hermes/自动化及安装版辅助功能诊断回读通过。

- 2026-08-28：已下线并删除并行实验版“AI工作岛 Next”。安装包、独立 Application Support 数据、SwiftPM target/source/test 和专用构建脚本已移入废纸篓，仍可恢复；稳定版 `/Applications/AI工作岛.app` 及其共用 Queue 续接实现保留。

- 2026-08-28：提升浅色模式右上角运行状态胶囊的可读性。浅色语义色现会与黑色混合 28% 压深，填充 alpha 由 `0.16 → 0.24`，描边改为同一语义色 34% alpha，避免亮蓝文字和淡蓝底在高亮桌面上融在一起。运行/等待/异常均沿用同一规则；深色模式不变。`verify_release_candidate.sh` 通过 257 项测试、视觉基线、构建安装、严格签名、单实例和数据源诊断。验收时临时切换浅色外观，结束后已恢复用户原有的“跟随系统”设置并重启单实例。

- 2026-08-28：修复深色模式在黄蓝高亮桌面上背景穿透过强、文字与边界难辨认。仅调整深色层级：外层面板 alpha `0.42 → 0.66`，任务卡 `0.64 → 0.74`，悬停卡 `0.72 → 0.79`，输入区 `0.72 → 0.84`，顶部状态胶囊填充 `0.16 → 0.28`；浅色模式不变，仍保持“外层最透、信息层次之、交互区最稳”。状态胶囊现会保留语义颜色并在外观切换时重新解析填充。重录深色胶囊视觉基线后，`verify_release_candidate.sh` 通过 257 项测试、视觉基线、构建安装、严格签名、单实例和数据源诊断。安装版在用户同一企微深色背景上实际悬停展开并截图回读：面板恢复中性深灰、卡片/输入区/顶部状态可分，背景黄蓝染色明显降低，玻璃折射仍保留。

- 2026-08-28：桌面胶囊的移动与展开命中区已分离。胶囊下方新增 26×6pt 独立拖动把手，按下后窗口按指针位移 1:1 移动，并有张手/握手光标、按压缩放反馈与“移动工作岛”辅助功能语义；胶囊主体不再承担拖动，鼠标进入即时展开，原 0.3 秒人为等待已移除。画布高度由 50pt 增至 56pt，顶部胶囊锚点不变，底部贴边改按把手的 3pt 实际可见边界计算。针对性交互、展开/回缩、锚点与构建测试通过；已构建安装到 `/Applications/AI工作岛.app`，严格签名、单实例、Hermes/自动化诊断通过，安装版全屏回读确认胶囊下方把手可见。全量 257 项第一次运行只因旧的 50pt 边缘 frame 断言失败，修正后该回归已通过；重跑时现有 `behindWindow` 离屏 PNG 快照受当前背景取样影响出现非确定像素差异，本次未擅自接受或重录既有基线。

- 2026-08-28：参考 `PixPin_2026-08-28_17-18-46.mp4` 的 BetterDisplay 菜单栏弹层，收紧桌面胶囊↔面板转场并拆分“玻璃外壳”和“可读内容”时序。展开由 340ms 收到 260ms，收回由 280ms 收到 220ms；外壳继续沿胶囊来源方向缩放/位移，真实内容先保持透明，延迟 50ms 后用 160ms 强 ease-out 淡入，收回时反向淡出，避免文字随窗口拉伸。内容延迟任务复用窗口转场 generation，悬停快速反向时旧任务不能覆盖新状态；“减少动态效果”仍即时切换。固定回归更新为 60Hz 至少 16 帧、120Hz 至少 32 帧，并检查内容淡入在外壳转场结束前完成、旧 generation 失效。`verify_release_candidate.sh` 已通过 257 项测试、视觉基线、构建安装、严格签名、单实例和数据源诊断。安装版另录制 4 秒 60fps 悬停展开/移开收回并逐帧回读：外壳先行、内容后入，收回同路，无文字拉伸、闪白、残影或旧 completion 回跳。

- 2026-08-28：移除展开面板自身的冷蓝染色。浅色/深色面板、任务卡、悬停卡、输入区和描边全部改为严格等值 RGB 灰阶；面板高光渐变的 `controlAccentColor` 蓝色停点改为中性白色，底层窗口折射与模糊、亮边、阴影和内容层级保持不变。蓝色仅保留在发送按钮、运行状态等语义强调控件。新增固定回归，逐一检查两种外观下五组表面色无蓝色通道偏移。`verify_release_candidate.sh` 已通过 257 项测试、视觉基线、构建安装、严格签名、单实例和数据源诊断；安装版浅色展开面板截图已回读，原蓝色洗色消失且文字、输入区边界正常。验收后恢复正常安装版进程。

- 2026-08-28：按 Apple Liquid Glass 层级重构工作岛材质，保留现有布局、尺寸与交互。外层面板与胶囊的 `NSVisualEffectView` 从 `.withinWindow` 改为 `.behindWindow`，真正折射窗口后的桌面；浅色/深色外层底色 alpha 降为 `0.30/0.42`，任务卡保留 `0.62/0.64`，输入区保留 `0.68/0.72`，形成“外层最透、内容次之、交互区最稳”的可读层级；胶囊也同步降低实体覆盖。新增固定材质回归，检查 behind-window 混合与三层 alpha 顺序，并重录 21 个胶囊视觉基线。`verify_release_candidate.sh` 已通过 256 项测试、构建安装、严格签名、单实例和数据源诊断；安装版浅色展开面板与胶囊截图已回读，桌面折射、边界和输入区可读性正常，深色由固定视觉回归覆盖。验收后已恢复正常安装版进程。

- 2026-08-28：针对 `PixPin_2026-08-28_17-06-58.mp4` 修复展开面板手动改变尺寸时背景分块、后置追赶。逐帧确认窗口与底色已跟随鼠标，但装饰性 `surfaceHighlightLayer` 的 `bounds/position` 仍被 Core Animation 默认隐式缓动约 0.25 秒。现对该层几何更新禁用隐式动画，保留胶囊↔面板正常转场；新增固定回归，连续模拟 `512×366 → 606×478 → 720×640` 三帧拖拽尺寸，每帧要求高光背景立即等于面板边界且无延迟动画键。255 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例和数据源诊断通过。Appium 仍因 `testmanagerd` 输入法授权跳过；真实指针拖拽手感可在用户下次改变面板尺寸时同时复验。

- 2026-08-28：修复完成摘要胶囊收回时偶发变成浅色长条。根因是窗口动画竞态可能已将外层恢复为 `120×50`，但内部仍保留 `260×56` 完成摘要约束；旧逻辑只比较外层尺寸后提前返回，造成宽内容被窄窗口裁切。现在外层尺寸与内部约束分别校验，任一不匹配都强制同步布局；新增竞态复现回归。254 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例和数据源诊断通过。安装版窗口回读为 `120×50`，直接截取该窗口确认可见内容已恢复为完整圆角胶囊“周 82%”，不再是长条裁切态。

- 2026-08-28：修复工作岛续接现有 Codex 任务时图片被 `codex queue` 拒绝。Codex CLI 0.150 虽仍显示 `queue -i`，但实际续接协议返回 `does not support image attachments`；稳定版和 Next 现将续接图片复制到 `~/Library/Application Support/AI工作岛/QueuedImages`，通过 Queue 明确要求原任务按本地路径读取，不再发送不受支持的 attachment。剪贴板临时图片仍及时清理，续接暂存图保留 7 天后清理。253 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例和数据源诊断通过。未向真实任务发送验收消息，避免业务副作用；下次真实图片续接可同时验收端到端读图结果。

- 2026-08-28：桌面胶囊↔面板的放大缩小升级为更细腻的源点式非线性过渡。展开从胶囊方向的 94% 尺寸、20pt 位移起步，用 0.34 秒强 ease-out 进入；收缩沿同路径回到 96.5% 尺寸、12pt 位移，以 0.28 秒快速回收。在 60Hz 下展开至少约 21 帧，120Hz 下至少约 41 帧；仍从当前窗口几何状态重定向，旧动画 completion 由 generation 隔离，快速反向不回跳。完成摘要尺寸 morph 调整为 0.46 秒，内容高度调整为 0.30 秒；“减少动态效果”仍直接切换。252 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例和数据源诊断通过。Appium 仍因 `testmanagerd` 输入法授权跳过，最终手感需在安装版上人工悬停/快速反向确认。

- 2026-08-28：彻底移除“有历史任务但分组全部折叠”时额度行下方的空白。高度计算已用 `showsEmptyState` 区分真空状态和折叠分组，但真实可见窗口仍被 `NSPanel.minSize=205pt` 强制撑高；现将窗口级最小高度收到折叠标题所需的 175pt，真正无任务时仍由内容计算保留 205pt 空状态。展开面板内任务出现、完成、分组展开/折叠引起的伸长和缩短使用 0.24 秒 ease-in-out 过渡，减少动态效果时即时调整。251 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例和数据源诊断通过；安装版 `--ui-review-panel` 回读确认额度行下方只保留正常圆角安全边距。

- 2026-08-28：收紧展开面板额度行下方的多余空白。根因是任务卡已改为真实测高后，面板仍沿用旧的 `172pt` 固定外壳高度；现按顶部边距、标题、三段间距、新建任务框、额度行和底部边距的真实约束改为 `153pt`，展开“最近完成”并显示两张普通卡时面板高度由 318pt 收到 299pt。新增对应布局回归；250 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例与数据源诊断通过。安装版 `--ui-review-panel` 实际截图确认额度行已贴近窗口底部，只保留正常圆角安全边距。

- 2026-08-28：修复完成态任务卡把 Codex 宿主界面指令（如 `::inbox-item{...}`）当普通正文显示。最终回答清理链现会整行移除符合 `::name{...}` 的宿主 UI 元数据，保留前后用户可读结论；新增真实 `inbox-item` 形态回归。249 项测试、视觉基线、补丁格式、构建安装、严格签名、单实例和数据源诊断通过；安装路径 `/Applications/AI工作岛.app`。Appium 仍因 `testmanagerd` 输入法授权跳过，本次文本过滤由真实指令形态回归覆盖，未向用户会话发送测试文本。

- 2026-08-28：修复 BetterTouchTool Hyper 模式下 `Caps Lock + R` 不触发全局录音。BTT 的 “Act as Hyper Key” 会把 Caps Lock 转成 `Control-Option-Shift-Command` 四修饰键，旧匹配器将其判为冲突；现同时接受原生 Caps Lock 标志和精确 Hyper 四修饰键，仍拒绝混合修饰、其他按键和按键连发。新增 Hyper 匹配回归；248 项测试、构建安装、严格签名与单实例通过。安装版真实发送 Hyper+R 后录音输入于 11:57:39 启动，再次触发于 11:57:53 停止并保留。

- 2026-08-27：放大任务卡关键操作而不增加卡片高度。“继续处理”从 small inline 改为 28pt 高、最小 76pt 宽的常规圆角按钮，字号提至 12.5pt；对勾、转到 Codex、打开产出和详情图标统一为 15pt 图形与 28×28pt 点击目标。新增操作尺寸回归；248 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例与数据源诊断通过。安装路径 `/Applications/AI工作岛.app`。当时无等待用户操作的真实任务，因此按钮实际悬停/点击视觉仍待下次真实状态出现时回读。

- 2026-08-27：全局录音快捷键由 `Option-Command-R` 改为 `Caps Lock + R`。Carbon `RegisterEventHotKey` 实测拒绝 Caps Lock 修饰位（`-9868`），因此改用 AppKit 全局/本地按键监视器：仅在 `R`、Caps Lock 标志开启、无 Command/Option/Control/Shift 且非按键连发时切换开始录音/停止并保留；菜单与错误提示同步更新。新增匹配规则及真实监视器注册/注销回归。

- 2026-08-27：修复独立 Codex 每周额度被 GPT-5.3-Codex-Spark 专属池覆盖。真实 rollout 中主额度为 `rate_limits.limit_id="codex"`，Spark 池为 `limit_id="codex_bengalfox"` / `limit_name="GPT-5.3-Codex-Spark"`，两者都使用 300/10080 分钟窗口；旧实现只按窗口长度分类，较新的 Spark 事件因此会被误认作账户周额度。现仅汇总 `codex` 主额度，仍兼容早期缺少 `limit_id` 的日志；新增 Spark 过滤与主额度保留回归。

- 2026-08-27：继续收紧展开面板两处残余空白。运行中 Codex 卡片不再使用“固定 92pt + 估算行高”，改为按标题、正文实际行高、阶段条、输入框、各段间距和上下内边距的真实约束逐项测高，截图同类单行运行卡由 110pt 收到 99pt；任务列表移除末尾可扩张占位视图，折叠“最近完成”后只保留分组标题，不再把面板余量塞在它与“新建任务”之间。新增单行卡真实栈高度、折叠分组高度及 arrangedSubview 数量回归。

- 2026-08-27：展开面板的任务分组默认状态改为“正在执行”展开、“最近完成”折叠；不再按首次出现的非空分组自动展开，因此只有完成任务时也不会把完成卡全部铺开。“等待系统”同样默认折叠，“等待你”继续因需要用户操作而常开，用户手动切换后的状态仍保留到本次进程结束。新增默认分组状态回归；全量 241 项测试、发布候选 8 项闸门、补丁格式、构建安装、严格签名、单实例与数据源诊断通过。安装路径 `/Applications/AI工作岛.app`。

- 2026-08-27：修复工作面板任务卡偶发多出大块空白。根因包含两层：首帧 Auto Layout 尚未产生宽度时曾按 1pt 测量结果；窗口调宽时又可能沿用任务栈上一帧旧宽度，且完成卡固定区重复计入 38pt。现以窗口当前宽度为测量真值，未就绪时回退到 426pt，并按标题、间距、输入框和内边距的真实约束重算固定区。新增零宽、旧栈宽/新窗口宽及截图同类长短结果回归；全量 241 项测试、发布候选 8 项闸门、严格签名、单实例和数据源诊断通过。安装版实际展开回读内容与输入框间距已收紧。安装路径 `/Applications/AI工作岛.app`。

- 2026-08-27：修复展开面板内多个输入框时，在“新建任务”粘贴图片却附加到其他任务卡的焦点错配。根因是 AppKit 会沿视图层级分发 `Command-V` 快捷键，而每个 `PastedImageTextField` 都会无条件拦截；现只允许窗口当前第一响应者对应的输入框消费粘贴。新增同窗口双输入框回归，验证旧任务框拒绝事件且图片只进入已聚焦的新任务框。全量 240 项测试、视觉基线、补丁格式、构建安装、严格签名、单实例、数据源诊断及发布候选 8 项闸门通过。安装路径 `/Applications/AI工作岛.app`。

- 2026-08-27：修复面板在卡片移除或内容缩高后偶发永久不回缩。根因是布局变化会开启“等待指针重新进入”的无限抑制；若面板已从指针下移走，用户必须先点击主界面再移出才能解除。现改为 0.6 秒有限布局宽限，宽限结束后 0.1 秒指针校正会按真实位置恢复 0.25 秒自动回缩，指针重新进入仍会立即取消待回缩。全量 239 项测试、发布候选 8 项闸门、严格签名、单实例、数据源诊断通过；安装版点击展开后在指针位于外部时 1.6 秒回读为胶囊。安装路径 `/Applications/AI工作岛.app`。

- 2026-08-27：桌面胶囊的鼠标移入与移开响应加快：悬停展开判定由 0.6 秒收到 0.3 秒，展开动画由 0.38 秒收到 0.24 秒；移开后的收缩等待由 0.5 秒收到 0.25 秒，刚展开时的面板移动宽限由 1.2 秒收到 0.75 秒，收缩动画由 0.28 秒收到 0.18 秒并改为快速缓出。保留拖拽判定、鼠标进入展开面板的容错和减少动态效果适配。239 项测试、视觉基线、补丁格式、构建安装、严格签名、单实例、数据源诊断及发布候选 8 项闸门通过。

- 2026-08-27：任务卡继续减轻字重：标题由 `.medium` 降为 `.regular`，当前进度正文也由 `.medium` 降为 `.regular`；当前状态只通过蓝色竖线和文字颜色区分。安装版截图回读已明显变细，项目名和历史正文仍为常规字重；239 项测试、视觉基线、补丁格式、构建安装、严格签名、单实例、数据源诊断及发布候选 8 项闸门通过。

- 2026-08-27：任务卡标题字重由 `.semibold` 降为 `.medium`，项目名由 `.medium` 降为 `.regular`；正文与状态栏字重不变。安装版实际截图回读层级仍清楚且不再发黑；239 项测试、视觉基线、补丁格式、构建安装、严格签名、单实例、数据源诊断及发布候选 8 项闸门通过。

- 2026-08-27：继续修复任务卡标题与项目名上下贴叠。上一轮横向左对齐后仍沿用压缩的 27pt 标题区和 -1pt 负间距，但 13.5pt 标题与 11.5pt 项目名实际需要约 30pt；现恢复 0pt 正常行距，单行/双行标题区调整为 32/48pt。安装版截图回读标题与“任务”上下间距正常，卡片正文和其他边距未改；239 项测试、视觉基线、补丁格式、构建安装、严格签名、单实例、数据源诊断及发布候选 8 项闸门通过。

- 2026-08-27：修复任务卡短项目名（如“任务”）在标题下方居中、与标题末尾形成错位重影。根因是 `NSStackView.alignment = .width` 会把短标签本身居中分配；标题和项目名现显式左对齐，垂直栈改为 `.leading`，并让两个标签等宽占满标题栈，兼顾统一左侧阅读线与长标题抗压缩。安装版实际截图回读“搜索衬线体 Codex 界面 / 任务”已左对齐；239 项测试、20 个普通胶囊与完成通知视觉基线、补丁格式、构建安装、严格签名、单实例、数据源诊断及发布候选 8 项闸门通过。

- 2026-08-27：稳定版任务卡与任务完成展开通知完成紧凑排版优化。Codex 卡片标题区由 30/47pt 收到 27/43pt，完成态减少固定高度、输入前后留白和上下内边距；状态点缩为 6pt，结果正文改用短竖线形成稳定阅读起点。完成摘要通知最终由 320×82pt 收到 260×56pt，使用 18pt 连续圆角、近白中性玻璃、弱发丝边框和更克制的 12.5/10.5pt 两级字阶；去除完成态蓝灰洗色，“完成环节”文案改为直接呈现阶段，普通折叠胶囊布局保持不变。浅色完成通知独立视觉基线已人工回读；239 项测试、构建安装、严格签名和补丁格式检查通过。

- 2026-08-26：稳定版“AI工作岛”现有 Codex 会话续接已与 Next 对齐为官方 `codex queue` 写入。卡片无论会话正在运行还是已完成，都以稳定 thread ID、cwd、文字和图片参数交给当前 Codex 会话所有者；运行中消息进入队列，不再调用私有 `thread-follower-steer-turn/start-turn`，也不再通过深链唤起 Desktop 后重试，因而不依赖 `app-server-control.sock` 或 follower handler。工作岛新建及仍由本进程持有的线程继续使用独立 App Server，原有逐线程退订与所有权转交保持不变。全量 238 项测试、发布候选 8 项闸门、严格签名、单实例、Hermes/自动化诊断和安装版胶囊可见回读通过；隔离测试线程真实 Queue 返回 `STABLE_PRODUCT_QUEUE_OK_20260826`。安装路径仍为 `/Applications/AI工作岛.app`。

- 2026-08-26：Next 会话侧栏已与 Codex 当前“置顶 / 最近”排布对齐。置顶从官方固定 `Pinned` section 按 `section_position` 原序读取；最近列表显式传 `modelProviders=[]` 纳入全部额度来源，按 `recency_at desc` 沿 `nextCursor` 拉完分页，并按稳定 thread ID 去重。当前 6 个旧置顶项已一次性迁移到官方 section，顺序保持为“工作岛接入app server、测算为什么跟罗老师对不上、学科业务、盘点AI提效工作、查找特训分析逻辑项目、核对暑2期百度渠道单量”。创建、改名、归档、恢复、turn 开始与结束触发刷新，另每 15 秒同步一次置顶变化；较旧并发结果由 revision 丢弃，当前选择与“新任务”状态按 ID 保留，Codex Desktop 切换会话时 Next 同步选中并屏蔽 follow 回声。安装版诊断读取 262 条非归档可见会话，`pinnedCount=6`、最近顺序 `orderedDescending=true`；完整 238 项测试、`git diff --check`、严格签名与新 PID 通过。安装版窗口 `/tmp/ai-next-main-window.png` 已回读“置顶 / 最近”标题、6 项顺序及最近首项。

- 2026-08-26：Next 的 Desktop 续接改为“官方 `codex queue` 写入 + `~/.codex/ipc/ipc.sock` 实时同步”，不再依赖 Desktop 生成 `app-server-control.sock`。现有会话发送文字、图片和文件路径时由本机 Codex CLI 0.149 的 `codex queue --thread` 交给真实会话所有者；Queue 接受后按 `thread/read(includeTurns)` 轮询到终态，避免第二 App Server 抢写锁。IPC 长连接使用当前 Desktop 的 4 字节长度帧协议，完成 initialize、follow、状态快照、断线重连和 Next 自有线程的反向 `conversationState` 广播；Desktop 或 Next 不需要额外启动服务。Desktop/外部所有的运行中或待批准会话允许继续提交 Queue 跟进，本地 App Server 所有的运行中会话仍禁止并发提交；中断只对 Next 本地所有会话开放，外部会话不再回退到第二 App Server。真实验收：当前 Desktop 会话 follow 返回完整快照；Next 所有者广播被独立 IPC 客户端收到；产品内 Queue 在独立线程完成并返回 `NEXT_PRODUCT_QUEUE_OK`。当前 Desktop 发布状态但对私有 `thread-follower-*` 写请求返回 `no-client-found`，因此它只保留为诊断，不作为产品写入主链。全量 233 项测试、`git diff --check`、安装版严格签名与新 PID 通过；Window Server 回读折叠胶囊为 144×52、点击后展开面板为 1165×616，IPC 诊断收到当前 Desktop 会话快照，近 10 分钟无新增崩溃。

- 2026-08-26：Next 已接入完整消息时间线与附件输入。`thread/read(includeTurns)` 会恢复用户、助手、命令、文件变更和 MCP 工具事件；实时增量按线程写入独立气泡，历史空回读不会覆盖刚提交的新消息。输入框支持 `Command-V` 粘贴文字或图片、拖入图片和普通文件、空文本 Delete 删除最后一个附件；图片按 App Server `localImage` 输入发送，普通文件以明确本地路径附入任务正文。修复时间线气泡在加入父视图前激活跨层级 Auto Layout 约束导致的安装版崩溃。安装版已真实完成一条含图片任务并返回 `IMAGE_INPUT_OK，我看到了图片。`；隔离构建目录全量 231 项测试、`git diff --check` 通过，修复后无新增崩溃报告。未迁移项：模型选择、工具详情展开，以及稳定版的额度和自动化聚合。

- 2026-08-26：按 Apple App / Liquid Glass 设计复审完成 Next 全量 UI 收敛。大标题由运行状态改为真实任务名，状态和项目路径降为次级上下文；正文从全屏玻璃改为冷白实体地面上的统一白色 18pt 会话面板，玻璃只保留在胶囊、标题栏和侧栏。侧栏当前任务使用单一系统蓝低透明选中底；新任务和工具按钮恢复图标加直接标签，发送为唯一蓝色主操作，输入与发送目标高度统一为 44pt。胶囊环境光从蓝靛双色收敛为单一系统蓝；展开和收缩都沿胶囊来源位置缩放/淡化，并以世代号隔离旧动画 completion。新增降低透明度和提高对比度适配。安装版真实截图已回读任务名标题、选中态、按钮层级和实体正文面，并验证自动收缩恢复胶囊。

- 2026-08-26：完成 Next 胶囊与展开面板的 Liquid Glass 视觉统一。窗口改为透明标题栏和全尺寸内容，侧栏使用较厚的系统 Sidebar 材质，正文区使用冷白玻璃底和极轻蓝靛环境洗色；会话列表改为标题/状态双层字级，正文容器使用 16pt 连续圆角、细分隔线和轻阴影，输入区与主操作统一为大号系统圆角控件。胶囊保留 Popover 材质，增加顶部折射高光、低强度蓝靛环境光、近白玻璃描边及浅深色自适应，不叠加彩色装饰卡片。安装版已回读新版胶囊和展开面板，并再次验证离开自动收缩。

- 2026-08-26：并行验证版“AI工作岛 Next”升级为胶囊入口的多任务工作台，不替换现有稳定版。安装路径 `/Applications/AI工作岛 Next.app`，Bundle ID `dev.kanyun.AIWorkIslandNext`。启动默认显示 132×40pt 跨空间悬浮胶囊；点击立即展开，悬停 0.6 秒展开，离开面板后按 0.5 秒和 1.2 秒移动宽限自动收缩，展开/收缩沿同一路径淡化，减少动态效果时直接切换。胶囊可优先拖动并持久化位置；重启时只有保存位置仍位于当前鼠标所在屏幕才复用，否则自动回到当前屏幕右上角，避免多屏切换后“进程启动但看不到”，再次点击 Dock App 也会找回胶囊。状态按审批、异常、运行、完成、空闲优先级显示。展开面板通过独立 `codex app-server --listen stdio://` 支持线程侧栏、新任务、项目目录选择、历史读取、线程恢复续接、运行中断、完成任务归档，以及命令/文件审批事件的允许或拒绝；每条线程独立保存运行状态，切换任务不会混写输出。只在 `~/Library/Application Support/AI工作岛 Next/thread-ids.json` 保存 Next 创建的 thread ID，不保存凭证或对话正文。真实安装版已回读胶囊、点击展开和离开自动收缩；多屏修复后 AX 窗口从副屏 `3032,228` 回到当前主屏右上角 `1752,48`。Next 专项 6 项协议/状态测试通过。当前固定模型仍为 `gpt-5.6-sol`。未迁移项：模型选择、图片、富工具详情，以及稳定版的额度和自动化聚合。

- 2026-08-26：修复会话卡标题区域被横向压缩到约一个汉字宽的问题。标题字符串和 `session_index.jsonl` 均完整，根因是 `WorkItemRowView` 的标题栈使用低水平抗压缩优先级，旁侧弹性占位会抢占宽度；现标题与标题栈使用 `.defaultHigh` 水平抗压缩，垂直栈按 `.width` 对齐，确保标题标签占满分配宽度并继续支持最多两行换行。完整 224 项测试、`git diff --check`、`build_and_run.sh --verify`、严格签名和安装版新 PID 通过。

- 2026-08-26：再次修复非运行卡片续接时反复出现 `thread ... already has an active writer`。安装版日志确认上一轮修复后仍在 13:17、13:18 复现：Codex Desktop 已持有写锁，但 follower IPC 暂时返回 `no-client-found`，旧修复仍会回退到第二个 App Server，必然撞写锁。现有会话不再回退到独立 App Server；遇到桌面通道不可用时，先按稳定 thread ID 唤起原 Codex 会话，让原 writer 恢复 follower handler，再做有限次数重试。新任务仍由独立 App Server 创建和执行。全量 224 项测试、`git diff --check`、构建安装、严格签名、单实例、Codex/Hermes/自动化诊断及安装版辅助功能回读通过；未向用户真实会话发送测试指令，避免业务副作用。

- 2026-08-26：修复已完成/待处理 Codex 卡片续接时返回 `no-client-found`。真实原因是 IPC 服务端会排除发起请求的工作岛客户端，再向其他客户端查找能处理该线程的所有者；因此上一轮将工作岛自身 `canHandle` 改为 true 不能修复该场景。现在仅运行中卡片使用桌面 IPC `steer`；非运行会话改用独立 App Server 的 `thread/resume` + `turn/start`，接受后立即转入后台监听，不阻塞卡片。新增路由回归，全量 223 项测试、构建安装、严格签名、单实例、Hermes/自动化诊断及 `git diff --check` 均通过。未向用户真实会话发送测试指令。

- 2026-08-25：统一桌面胶囊与展开面板的 Liquid Glass 运动参数。展开沿胶囊→面板同源路径使用 0.38 秒高阻尼感缓出，收缩沿同一路径使用 0.28 秒回收；悬停、按压与尺寸变化统一为 0.14/0.32 秒短过渡。状态胶囊从 Layer 的当前呈现值继续弹簧回正，快速状态切换不再从固定初值重启。仍保留“减少动态效果”下的无位移切换。浅色玻璃回归时同步将旧烟灰调色断言改为当前自适应玻璃常量。`swift test --disable-sandbox --scratch-path /tmp/ai-work-island-resume-20260825-full`（220 项）、`build_and_run.sh --verify`、`verify_release_candidate.sh`、固定要求的 ad-hoc 签名、单实例、数据源诊断与安装版辅助功能回读均通过；Appium Mac2 仍受 `testmanagerd` 输入法授权限制，连续鼠标手感保留人工验收项。

- 2026-08-25：用户选择保留 Git `ee380eb` 的浅色半透明胶囊视觉。仅恢复 `NSVisualEffectView(.popover)`、浅色边框与蓝白高光图层；当前完成摘要、状态轮询、额度刷新、悬停拖拽、输入与图片粘贴等功能实现全部保留。因当前功能已新增完成态的双行布局，20 张基线按旧材质结合当前状态重新录制，而非盲目覆盖旧像素。两次独立缓存的 `swift test --disable-sandbox`（219 项）、`./script/verify_release_candidate.sh`、`git diff --check`、安装构建、严格签名和单实例通过；Computer Use 已从安装版回读浅色胶囊。Appium Mac2 仍因触发 `testmanagerd` 输入法授权不纳入自动闸门。

- 2026-08-25：完成本轮面板可靠性与输入交互收尾。状态轮询会在成功应用快照后校验 Timer 存活，并把公司额度刷新挂到稳定的常规刷新循环，避免长期停在会话标题同步中或旧额度。任务进度显示改为精简最近 4 条、详细最近 6 条；运行任务突出最新公开进度，完成任务保留完整结果。新任务与卡片续接输入框均验证 `Command-V` 图片粘贴、仅图片发送和空文本退格逐张删除；图片只存在系统临时目录，发送或删除即清理。重录 20 个胶囊视觉基线。`swift test --disable-sandbox --scratch-path /tmp/ai-work-island-tests-continue`、`git diff --check`、`build_and_run.sh --verify`、严格签名、单实例、数据源诊断与发布候选闸门均通过（219 项测试）。Appium Mac2 仍因触发 `testmanagerd` 输入法授权而不纳入自动闸门，实际悬停/拖动/图片发送交互需在用户允许时用 Computer Use 回读。

- 展开面板的顶端与胶囊可见本体顶端对齐；内容高度变化时只向下伸缩，零任务空态不再围绕面板中心向上下同时收缩。水平位置仍沿用默认居中或用户拖动后的记忆位置，超出当前屏幕时继续自动夹回可见区域。

- 面板高度按当前可见内容自动伸缩。零任务空态外框为 224pt，仅一条普通完成任务为 246pt，不再受旧 320pt 下限强制撑高；任务增加或正文变长时仍按内容增长并受最大高度保护。

- 菜单中的信息密度改为“任务进度显示”，选项直接说明“精简 · 最近 4 条进度 / 详细 · 最近 6 条进度”；Codex 任务尚无公开进度时显示“已连接 Codex，正在等待首条进度”，避免用“正在思考”混淆连接状态与隐藏推理。

- 迷你胶囊动效采用克制的 macOS 原生工具节奏：状态内容 0.24 秒淡化、运行态只保留状态点轻呼吸与低强度扩散，不再持续扫光或旋转描边；状态切换不缩放整颗胶囊。出现/退出使用小幅位移和临界阻尼弹簧，悬停与按压保持即时反馈，并遵守系统“减少动态效果”。

- 展开面板位置与收缩胶囊位置相互独立：拖动展开面板只持久化面板中心，不再改写胶囊锚点或移动胶囊；拖动胶囊只更新下次收缩位置。胶囊展开时，面板继续恢复到用户上次移动后的中心位置。
- 胶囊悬停展开前保留 0.6 秒拖拽判定窗口；在此期间按住并拖动会取消悬停展开、优先移动胶囊，单击仍直接展开。该时长覆盖真实鼠标先定位再按下的延迟，不影响单击立即展开。
- 常驻刷新器同时检查轮询间隔和 Timer 有效性；每次成功应用状态快照后都会确认刷新器存活，避免面板永久停在会话刚启动时的“…”、“任务”和“正在思考”兜底帧。
- 会话卡大标题由单行尾部省略改为最多两行自动换行；短标题区收紧为 30pt，只有长标题才按实际宽度增高到 47pt。标题与项目名间距归零，标题到正文及卡片上下内边距同步收紧。
- 会话索引尚未同步时，纯省略号或纯标点不再被当作真实标题；Codex 卡片明确显示“会话名称同步中”，并继续展示由 cwd 得到的真实项目名。

- 项目及本地仓库名称为“AI工作岛”（SwiftPM：`AI工作岛`）；为兼容既有设置、辅助功能授权和自动化链路，Bundle ID、可执行文件名及 `~/Library/Application Support/Codex Hermes Touch Bar/` 兼容数据目录不改名。
- 桌面面板先展示当前状态与任务卡，再提供紧凑的“新建任务”输入框和合并额度行；展开时不自动抢占输入焦点。新任务回车或发送按钮直接创建会话；默认工作目录为“文稿”，也可通过文件夹按钮选择并持久化为后续新任务目录。
- “新任务”和卡片内“继续该项目”输入框均支持 `Command-V` 粘贴剪贴板图片；输入框显示图片数量，允许仅图片发送。输入框没有文字时按 Delete/退格会逐张删除最后粘贴的图片。图片暂存于系统临时目录，通过 Codex `localImage` 输入随文字一并发送，发送或删除后清理，不持久化到项目或会话状态文件。
- Swift 6 + AppKit。
- 用户可见名称统一为“AI工作岛”，安装路径为 `/Applications/AI工作岛.app`。为保留既有设置、辅助功能授权和自动化链路，Bundle ID、可执行文件名及 `~/Library/Application Support/Codex Hermes Touch Bar/` 兼容数据目录不改名。
- `WorkStatusHub` 将 Codex 线程与自动化任务统一为 `WorkItem`；桌面面板和 Touch Bar 继续各自按信息密度展示。
- `DesktopStatusPanelController` 提供跨桌面空间、可移动的桌面面板；默认使用始终置顶浮窗，可从菜单切换为置于普通窗口后方。最多展示 7 项任务，等待确认、异常和失联优先。公司与 Codex 额度合并为任务区下方的一条低权重状态行，任一额度低于 50% 才转橙、20% 及以下转红。
- 桌面面板内置项目卡片对话：每张可见 Codex 卡片都有独立输入框，发送时按该卡片稳定 thread ID 续接对应会话；卡片从 rollout 只读提取最近一次 `final_answer`，去除 Markdown/记忆引用并压缩为单行“上轮”摘要。Desktop 或外部所有的现有任务通过官方 `codex queue --thread` 写入，运行中消息由真实会话所有者排队处理；新会话及仍由工作岛持有的线程通过官方 Codex App Server 的 `thread/start`、`thread/resume` 与 `turn/start/steer` 执行，固定 `serviceName=codex-desktop`、`threadSource=vscode`、`ephemeral=false`，保证进入 Codex 持久线程索引。工作岛创建的 thread ID 最多保留 100 个在兼容数据目录的 `work-island-thread-ids.json`，完成后继续显示为待读，避免被仅“活跃或未读”扫描规则过滤；不保存 Token、Cookie 或对话正文。
- 运行中的 Codex 卡片从 rollout 只读提取左侧已经公开显示的助手正文，包括 `commentary` 和当前 Codex 未标 phase 的 assistant message；按原顺序保留最近 4 条多行正文。工具调用和命令不进入卡片正文，也不会挤掉公开说明。若会话明确调用计划更新，同时单独显示真实 `已完成/总步骤` 和分段进度条。无公开事件时显示“正在思考”；不读取或伪造隐藏推理过程，每次新 turn 清空上一轮运行进度。
- 会话卡片始终占满面板可用内容区。标题 14pt 半粗，项目归属与分组信息不小于 11.5pt；所有事件和完成结果通过显式等宽约束统一从左侧起读。等待、异常和失联卡常驻“继续处理”主操作；产出、标记已阅和详情等次级操作仍在悬停时显示。面板默认及最小宽度为 480pt，可调整范围为 480×320pt 至 720×820pt；运行卡清爽/详细模式最多显示 4/6 条公开事件，历史与当前动态分别限制为 1/2 行；完成结果不截断，并按字体与正文实际可用宽度测量卡片高度。
- 桌面面板不再提供“缩小为悬浮按钮”入口，应用启动时默认显示 108×38pt、跨空间、可拖动的迷你灵动岛胶囊，外层窗口使用 120×50pt 透明动画画布。运行态使用状态点呼吸、外圈扩散、背景光带和无缝旋转渐变描边；状态变化使用文字推入、状态点弹跳、色环扩散、胶囊弹性和边框闪光；缩小与恢复使用窗口交叉淡化及胶囊弹簧进出。悬停与按压保留缩放反馈，全部动效遵守系统“减少动态效果”。点击胶囊只恢复面板，不打开最近完成任务的 `openURL`、`outputPath` 或 Finder；外部目标必须由任务卡上的明确操作打开。
- 迷你胶囊每 4 秒按“任务状态 → 公司剩余百分比或待配置 → 可用的 Codex 5 小时额度 → 可用的 Codex 周额度”轮播；公司完整额度固定按 $200 = 100% 换算，主面板、胶囊和 Touch Bar 均只展示剩余百分比。公司额度是默认主额度，Codex 缺失时不占位。额度沿用 `WorkStatusSnapshot` 与面板相同的只读来源和失效结果，不额外缓存。异常、失联或等待确认时锁定任务状态，不轮播额度。额度轮播只做文字推入，不触发整颗胶囊的状态弹跳；正常额度使用青/靛/紫，低于 50% 橙色、20% 及以下红色。
- 迷你胶囊拖到当前屏幕的可用区域边缘 18pt 内时自动贴边；展开主面板最终固定在鼠标所在屏幕的可用区域中心。展开与回缩先沿胶囊方向偏移 18pt 再交叉淡化到目标位置，建立来源关系；系统开启“减少动态效果”时直接切换。胶囊按可见的 108×38pt 本体贴边，保留其 6pt 透明动画画布。
- 点击或悬停胶囊展开面板后，提供最长 1.2 秒的指针移动宽限；进入面板后沿用离开 0.5 秒自动回缩。若回缩时鼠标仍在胶囊将出现的位置，胶囊会等待真实移出后才允许再次 hover 展开，避免“居中面板回缩 → 胶囊在鼠标下重现 → 再展开”的持续闪烁闭环；点击始终可展开。打开 Codex 会话时会在本地 `viewed-thread-ids.json` 记录查看时间。
- 展开面板首次仍位于当前屏幕中央；用户拖动展开面板后，后续悬停或点击展开会恢复该位置，并跨应用重启持久化。位置记忆使用面板中心点而非左下原点，因此任务变多、详细模式或内容自适应导致面板增高时仍围绕用户位置重排，并自动夹回当前可用屏幕。外接屏断开或分辨率变化导致旧位置不可用时同样收回可见区域；呈现动画和自动布局不会覆盖用户位置。
- 任务完成不再自动展开主面板；折叠态在原右上锚点把 108×38pt 胶囊扩大为 320×76pt 摘要胶囊，显示任务名及完成步骤、最后环节或结果首行，6 秒后平滑恢复。悬浮或点击摘要胶囊仍进入同一完整面板；录音状态优先于完成摘要。
- 每张 Codex 任务卡悬停后提供“转到 Codex”。工作岛自建任务通过共享的独立 App Server 并发执行：每个 turn 完成或主动转移时先发送 `thread/unsubscribe`；最后一个工作岛任务结束后自动关闭共享服务，从进程层释放已加载 thread，再用 `codex://threads/<thread-id>` 按稳定 ID 打开最近聚焦的 Codex 主窗口。工作岛界面曾在任务中重启时，点击完成会话也会清理 launchd 遗留服务。仍有其他工作岛任务执行时不会关闭服务或误弹 Codex 错误，而是提示并等待服务空闲后自动跳转，避免中断并发任务。当前 ChatGPT/Codex 版本可能只向辅助功能树暴露原生窗口外壳，因此跳转成功不再依赖正文标题或侧栏行的 AX 回读；若能读到明确的“未找到对话”仍按失败处理。
- `等待你` 分组固定排在所有分组之前、始终展开且不能折叠；其中的任务逐条显示并可占满 7 个可见位置，不与运行中或最近完成任务合并。每张 Codex 待读卡可单独“标记已阅”，分组标题可“全部已阅”；批量写入同一个已阅时间点，刷新后胶囊和分组计数清零。卡片的“转到 Codex”用于查看结果并继续处理。
- 桌面面板通过 `DesktopPanelContentSignature` 忽略仅刷新时间或任务 `updatedAt` 变化的轮询；额度使用独立摘要视图原位更新，不重建任务行。标题、详情、状态、阶段、按钮目标或分组确实变化时才刷新任务内容。
- `AutomationStatusScanner` 只读 `~/Library/Application Support/Codex Hermes Touch Bar/automation-status/*.json`；运行状态默认 30 分钟未更新转为失联，可由 `staleAfterSeconds` 覆盖或设 0 关闭。
- 会议纪要服务已通过同一只读 JSON 协议接入：面板卡片展示监听中、转写、生成纪要、写入 Obsidian、推送排队、已转写待生成和异常；状态文件不包含录音、转写稿或纪要正文。
- 菜单保留 Touch Bar/面板显示方式、自动化状态目录、全局录音、重启和退出；任务卡负责打开 Codex 会话或自动化产出。
- 录音守护 V1 每 5 秒通过辅助功能只读判断语音备忘录是否显示“暂停”，录音期间以红色 `录音 N分` 覆盖胶囊轮播；AVAudioEngine 仅在内存统计音量，连续 5 分钟低于 mean -45 dB 且 peak -30 dB 后显示橙色 `静音 N分`。录音胶囊以七柱实时波形替代原红色状态点，复用现有 4096 帧音量回调，只传归一化音量值，不保存 PCM 或历史音频；静音时波形趋平并转橙，录音结束立即隐藏并清零。提醒提供“结束并保留 / 继续录音”，15 分钟内不重复提醒；绝不自动停止、删除或保存音频内容。系统通知不可用时回退为应用警告框。
- `RolloutScanner` 读取 Codex 本地状态。
- `HermesStatusScanner` 仅供 `--diagnose-hermes` 手动诊断，常驻刷新不再读取 Hermes 状态。
- `CompanyQuotaScanner` 启动时立即读取一次，之后由独立定时器每 5 分钟通过 Edge 页面内同源请求读取 `/api/v1/users/self`；请求带防缓存参数，失败时保留最近一次成功值，不提取 Cookie。
- `TouchBarController` 只展示 Codex 任务状态、Codex 剩余额度和公司额度，不提供推理程度控制。Codex 胶囊读取 rollout 的 5 小时与周窗口，数据源只提供其中一项时单项显示；公司胶囊按 $200 满额只显示剩余百分比。
- 两个额度组件展示剩余百分比；50% 以下橙色、20% 以下红色。
- 主面板支持跟随系统、浅色和深色三种外观。外层只保留单层系统材质，不再叠加装饰性渐变高光；卡片和输入框使用高不透明实体表面，外层描边与阴影减弱。主题切换时动态 `NSColor` 必须按窗口外观解析。状态色只承担运行、待读和低额度等语义。本轮按用户要求不做“减少透明度 / 提高对比度”专项适配。
- 任务卡支持持久化的“清爽模式 / 详细模式”，从菜单“任务信息密度”切换。清爽模式使用 104pt Codex 卡片和单行 11pt 摘要；详细模式使用 136pt 卡片、11.5pt 高对比提示文字和最多三行摘要，将“进度 / 当前 / 最新”分行。正文、路径、状态和输入提示已提高字号或对比度；蓝色仅保留状态点、进度、按钮和悬停边框，悬停只轻微提亮卡面、不再整卡泛蓝。未保存过选择的新用户默认清爽模式，当前本机已选详细模式。
- 主 Touch Bar 依次展示任务、公司额度和 Codex 额度；公司额度未配置时显示待配置，Codex 额度仅在有数据时呈现有效值。
- Codex 状态每次轮询都会重算；零任务时显示“Codex 空闲”，否则只显示非零的处理中/待读数量。处理中按 `isActive` 会话计数，待读按 `isUnread` 会话计数。
- `RolloutScanner` 对缺少 `task_complete` 的异常日志增加 30 分钟活跃失效时间，避免历史 `task_started` 永久占用处理中计数。
- 菜单栏设置、状态提示和用户可见错误统一使用中文；诊断命令仍保留英文键名，方便脚本解析。
- 无开发者证书时使用固定 Bundle ID 的 ad-hoc designated requirement，避免默认 `cdhash` requirement 随每次构建变化；首次切换后仍需重新添加一次辅助功能权限。
- 公司额度扫描按进程选择 `activationPolicy == .regular` 的常规 Edge，通过 ScriptingBridge 在现有 `model.zhenguanyu.com` 标签页执行同源请求；不会误连直播监控或截图使用的无头 Edge，不读取 Cookie，也不切换用户当前标签页。读取失败时保留上次成功缓存。
- App 默认仅在 Codex 前台时展开；菜单可切换“始终显示（所有应用）”，状态持久化在 `touchBarAlwaysShow`。
- 菜单提供“重启应用”：先启动延迟 0.5 秒的本地重开任务，再退出当前进程，避免单实例保护拦截新实例。
- Touch Bar 被关闭按钮收起后，再次打开 `/Applications/AI工作岛.app` 会通知既有后台进程强制恢复仪表盘，不再被单实例保护静默吞掉。
- “处理中”和“待读”、整张 Codex 任务卡及悬停“转到 Codex”是三个用户入口，但必须共用一条打开状态机：本地注册的工作岛 thread 先等待其他工作岛任务结束、退订并停止共享 App Server，再按稳定 thread ID 打开；普通 Codex thread 才可直接按稳定 ID 打开。不要恢复标题搜索自动化，也不要让任何入口绕过工作岛所有权判定直接发送深链。
- App 图标母版位于 `Resources/AppIcon-master.png`，构建使用 `Resources/AppIcon.icns`；银色连续圆角外壳由黑色 Touch Bar 横带贯穿，中央鼓起为含三枚状态点的工作岛。菜单栏图标由 `StatusItemIconRenderer` 以同一结构绘制成 18pt 系统模板图标，自动适配浅色与深色菜单栏；`script/generate_app_icon.sh` 可从母版重建全尺寸 ICNS。
- macOS 27 动态图标工程位于 `Resources/AppIcon.icon/`：烟灰玻璃外壳、中央工作岛、蓝/青/琥珀状态点均为独立 SVG 图层，`icon.json` 定义分组光照、specular、blur material、translucency、阴影及 light/dark 系统底色。工程已由 Apple Icon Composer 2.0 打开、预览和保存规范化；构建同时打包原生 `.icon`、声明 `CFBundleIconName=AppIcon`，并保留由官方 Default rendition 生成的 ICNS 供 macOS 26 及旧系统回退。
- 构建目录默认放 `/tmp`，避免 iCloud 路径中的 SwiftPM 锁竞争。
- 构建产物保留在 `dist`，实际运行副本安装到 `/Applications`，避免 iCloud 改写 Bundle 元数据。
- 内置 GitHub 更新提醒：启动 8 秒后检查 `Neo-niu/ai-work-island` 最新正式 Release，成功检查后 24 小时内不重复；菜单提供“检查更新…”。新版弹窗支持查看下载、稍后提醒和跳过此版本，不自动覆盖应用。`GitHubReleaseTag` 用于兼容现有日期标签；后续发布应改用与 App 版本一致的标签，并由 `build_release.sh` 写入安装包。
- 签名先在系统临时目录完成，再复制回 `dist` 和 `/Applications`；否则文件提供器会立即给 app 根目录补写扩展属性，导致 `codesign` 报 resource fork/Finder information 错误。
- TowerIsland v1.3.4 作为独立试用应用安装在 `/Applications/Tower Island.app`，没有并入本项目源码。仅启用 Codex 自动配置，bridge 位于 `~/.tower-island/bin/di-bridge`，其他 Agent 自动配置关闭，PATH 未修改。原 Codex hooks 备份位于 `/Users/kanyun/Documents/Codex/2026-08-11/TowerIsland-backup-20260811-164813/codex-hooks.json`。

## 最近回归

- 2026-08-27：修复自动化任务完成后偶发把工作岛撑成白色错位区块。真实日志确认同一 `WorkItemRowView` 同时收到自动化卡固定 56pt 与误套 Codex 对话结果算法产生的 127pt 高度；现统一在高度策略入口将非 `codex:` 卡固定为 56pt，并删除卡片内部的重复高度约束。新增完成自动化卡高度与 extraHeight 回归；239 项测试、构建安装、严格签名和补丁格式检查通过。安装版以现有完成自动化卡展开回读为 522×374pt，画面正常，当前 PID 日志无 `WorkItemRowView` 布局冲突。

- 2026-08-27：修复超长完成结果偶发把工作岛撑成白色巨块/错位矩形。真实安装版日志曾出现完成正文活动行高 1365pt、任务卡高 1478pt，与最高 820pt 面板互相冲突；完成结果现将可见区域限制为 360pt，超出部分在卡片内滚动，正文不丢失，卡片整体不再突破窗口布局上限。新增极长结果高度上限回归。

- 2026-08-26：修复“核对暑2期百度渠道单量”等会话标题在工作岛只显示首字的问题。`~/.codex/session_index.jsonl` 和 SQLite 均保留完整标题，显示异常来自标题栈的水平压缩与 `.leading` 子视图对齐；改为高水平抗压缩和 `.width` 对齐后，标题使用完整可用宽度并最多两行换行。完整 224 项测试、补丁检查、安装构建、严格签名和新进程验证通过；安装版窗口捕获正常，但当前选中项目无可见任务卡，因此未把目标标题截图记为已完成的黑盒视觉验收。

- 2026-08-25：修复会话卡大标题单行省略和标题区留白过多。标题改为最多两行自动换行，短标题区固定 30pt，只有实际放不下时增高到 47pt；标题与项目名间距归零，标题到正文及卡片上下内边距同步收紧。完整 219 项测试、`git diff --check`、`build_and_run.sh --verify`、严格签名、安装版新 PID、进程与诊断回读通过；安装版辅助功能树已回读完整标题“继续指定 Codex 线程任务”和项目名“AI工作岛”，未出现省略号。

- 2026-08-24：展开面板改为与胶囊可见本体顶端对齐。内容高度变化时固定顶边、仅向下伸缩，解决零任务空态围绕面板中心上下同时收缩的问题；默认水平居中和用户拖动后的水平位置保持不变，跨屏或越界仍夹回胶囊所在屏幕。新增空态与高面板同顶回归；完整 219 项测试、胶囊视觉基线、`build_and_run.sh --verify`、安装版新 PID、严格签名、Hermes/自动化诊断及 `git diff --check` 通过。

- 2026-08-23：修复工作岛输入框点击后无法输入中文。根因是应用以 accessory 模式运行，面板通过 `orderFrontRegardless` 展示时原应用仍可能保持激活；输入框虽然获得视觉焦点，但中文输入法的 marked-text 输入上下文没有切到工作岛进程。现 `PastedImageTextField` 在处理鼠标点击、创建 field editor 前先激活当前应用并让面板成为 key window；新任务与卡片回复输入框共用此修复，并新增中文 marked-text 组合与提交回归测试。

- 2026-08-22：修复展开面板顶部大标题在高密度内容下上下裁切。根因是任务卡已设为垂直不可压缩，但主标题栏仍使用默认压缩优先级；当内容逼近窗口最大高度时，AppKit 会优先压缩顶部标题栏。现为 18pt 主标题、状态胶囊和标题栏设置垂直必需抗压缩，并为标题栏保留至少 24pt 高度；不改变面板整体高度或用户已有尺寸。新增展开面板主标题高度回归；完整 215 项测试、`build_and_run.sh --verify`、安装版新 PID、严格签名、Hermes/自动化诊断及 `git diff --check` 通过。安装版辅助功能应用树可回读，但系统增强辅助功能属性仍返回 `-25205`，因此本轮未把 AX 标题几何回读冒充为视觉验收。

- 2026-08-22：去除无活动任务或仅少量完成任务时的面板底部留白。根因是零任务被强制按一条 62pt 任务计算，窗口又固定最短 320pt；现零任务按真实 52pt 空态计算，并将最短高度收至完整空态所需的 224pt。

- 2026-08-22：修复切换显示器布局或降低分辨率后展开窗口不可见。根因是旧展开高度 `820pt` 超过当前主屏 `726pt` 可用高度，而窗口可见性规则此前只移动 frame、不压缩过大的 frame，导致边界计算反转。现按目标屏幕可见区域同时约束窗口尺寸与位置；胶囊透明动画边距仍保留。新增当前真实 `1504×846` 缩放场景回归测试；完整 217 项测试、构建安装、严格签名、新进程和自动化诊断通过。安装版从菜单真实展开后，Window Server 绑定新 PID 回读面板为 `(1024, 330, 480×426)`、`onscreen=1`，完整位于主屏可见区域。
- 2026-08-22：完成真实鼠标胶囊验收。用户手动执行“拖动 → 顶边吸附 → 悬停展开 → 进入面板 → 移出回缩”，随后 Window Server 回读可见胶囊画布稳定为 `(1046, 30, 120×50)`；按 6pt 透明动画边距换算，可见本体顶边精确贴合主屏可用区域。Computer Use 再次点击可展开完整面板，自动回缩后主面板为 offscreen、胶囊为 onscreen，回缩 frame 与手动操作后的 frame 逐点一致。

- 2026-08-22：尝试用 Computer Use 完成胶囊真实鼠标验收。实际发现其拖拽前的指针定位会先触发 0.35 秒悬停展开，已将拖拽判定窗口延长为 0.6 秒；213 项测试、安装构建和真实折叠态回读通过。修复后 Computer Use 对无边框非激活 NSPanel 仍只能触发悬停展开，不能生成 AppKit 可识别的 `mouseDragged`，Window Server frame 保持不变，因此“拖到边缘 → 吸附 → 展开 → 回缩”仍需用户真实鼠标操作一次，不能以自动化冒充通过。

- 2026-08-22：完成详细模式状态提示优化。模式选项直接写明可见进度条数，任务刚启动且尚无公开正文时明确提示已连接并等待首条进度；不推断隐藏推理或虚构执行阶段。

- 2026-08-22：按苹果原生 App 的克制原则收敛工作导师胶囊动效。移除运行态持续背景漂移和旋转描边、状态切换的整颗胶囊弹跳与扫光；文字改为 0.24 秒淡化，状态点呼吸幅度与扩散强度降低，出现/退出缩放范围收至 0.94–1，并统一使用强 ease-out / 临界阻尼节奏。减少动态效果路径继续直接切换。

- 2026-08-21：降低主面板底部额度摘要的视觉权重。临界额度仍保留红色语义，但从饱和系统红改为 68% 强度；50% 以下的橙色同步收敛到 78%。只影响主面板底部摘要，胶囊、Touch Bar 和错误状态继续使用原有强警示色。

- 2026-08-21：按用户截图修复完成结果卡正文被截断、上下留白偏多。最终答复读取不再截到 1200 字，完成态正文也不再受清爽 4 行/详细 8 行上限限制；初次渲染与异步状态更新统一使用无限行。卡片和正文共用 AppKit 12.5pt 系统字体，并按面板当前内容宽度测高，不再使用固定 390pt 窄宽或字符数二次估算；正文提高垂直 hugging，回复框紧随正文，完成卡上下内边距和区块间距同步收紧。新增超长结果不截断、宽度改变影响高度、完成态无限行等回归。胶囊视觉测试改为递归清理整棵图层树的动画后再截图，当前保留实现的 20 个基线重录后连续两次逐字节复验通过；完整 208 项 Swift/视觉测试、`git diff --check`、`build_and_run.sh --verify`、单实例及 Hermes/自动化诊断全部通过。安装版 `--ui-review-panel` 的 AX 回读确认真实面板使用新版宽正文和紧邻回复框，并通过 AX 元素成功展开“最近完成”分组；当前目标只有结束本轮后才会成为完成卡，因此无法在同一轮回读该卡的最终态。

- 2026-08-21：修复公司额度常驻运行时停留旧值。真实复现为同一 Edge 登录态下独立诊断已读到 59%，运行中胶囊仍显示缓存的 86%；现将额度刷新同时挂到已稳定运行的常规状态轮询，`CompanyQuotaScanner` 继续按 5 分钟节流真实请求，专用额度定时器被系统合并或漏触发时也能自动恢复。

- 2026-08-21：完成发布前剩余验收。双屏环境为 4K 外接主屏与内建 Retina 副屏；屏幕重配、全屏层级、失效位置回收及四边吸附回归通过。跨重启任务实测中，工作岛 UI PID 从 34953 变为 44328，独立 App Server PID 41987 保持不变，同一线程最终回读 `RESTART_CONTINUITY_PASS`。精确会话跳转以稳定 thread ID `01a023f4-9021-7790-b7a1-cef000920976` 交叉核对工作岛注册表、Codex 索引和深链，统一入口与所有权释放回归通过。正式 ZIP 干净环境首装按用户要求移出范围。Appium Mac2 因仅建立会话也会触发 `testmanagerd` 第三方输入法授权，已从发布闸门移除；输入与交互使用 Swift 回归、Computer Use 和系统状态证据验证。完整发布闸门通过：206 项 Swift/视觉测试、`git diff --check`、安装构建、严格签名、单实例和数据源诊断通过。

- 2026-08-21：压缩运行中单任务卡的垂直占用。仍保留最近 4 条公开正文及原顺序，但历史三条各显示 1 行，最新一条显示 2 行并保留蓝色当前态强调；不通过缩小标题或正文字号换空间。卡片高度计算与实际行数同步，避免留下空白。

- 2026-08-21：修复运行卡正文较多时顶部大标题只剩半截。根因是固定卡片高度未完整计入标题头部，纵向 Stack 在正文占满空间后压缩了首行；现为标题头部保留 35pt 不可压缩高度，并同步增加卡片基础高度。新增高密度正文下的标题空间回归测试。

- 2026-08-21：修复右侧任务卡被工具命令占据、未直接呈现 Codex 左侧正文的问题。rollout 读取现在接受当前 Codex 无 phase 的 assistant message，并完全排除工具调用文本；增量刷新会清理旧的命令型活动，只保留最近 4 条公开正文。204 项 Swift/视觉测试、安装构建、严格签名、单进程、Hermes/自动化/辅助功能诊断与 `git diff --check` 通过。安装版辅助功能树在“暑2下探1”卡片按原顺序回读到“六份补拉已启动……”和“首份因 Spark 坏节点……”，没有命令行占主体；当前任务卡也同步显示本轮全部公开正文。

- 2026-08-21：项目名称、用户可见 App 名与本地仓库文件夹统一为无空格的“AI工作岛”；SwiftPM 元数据、安装路径、脚本、文档、辅助功能标签和对应测试同步更新。为延续已有权限、设置及本地数据，Bundle ID、可执行 Target、launchd 标签和兼容数据目录不改名。

- 2026-08-21：修复记住展开位置后面板动态增高可能越出屏幕、只露出一块矩形的问题。原实现持久化左下原点，内容从短面板增长到高面板时顶部会越界；现改为持久化中心点，每次展开或内容改高均按新尺寸重建 frame 并夹回可用屏幕。旧的 `desktopPanelUserOriginX/Y` 不再读取，首次升级自动回到屏幕中央，用户下一次拖动后写入新的中心点。排查期间确认 Computer Use 会给无边框窗口绘制浅灰目标高亮，不能当作 App 像素；CoreGraphics 原始窗口捕获的胶囊四角 alpha 为 0、非透明区域严格为 108×38pt 本体。排查性外观改动已撤回，20 个胶囊视觉快照恢复通过；完整 201 项测试通过。

- 2026-08-21：进一步消除胶囊外层矩形。悬浮窗口不再使用 Window Server 的矩形窗口阴影，胶囊本体也不再依赖 `NSVisualEffectView` 的独立 backdrop 合成，改为透明 `NSView` 上的圆角实体玻璃层、现有高光与状态动效；因此 120×50pt 动画画布之外保持透明，视觉只剩 108×38pt 圆角本体。20 个胶囊视觉基线已按最终自绘实现重录；安装版 Window ID 原始 PNG 四角 alpha 均为 0，中心胶囊像素有效。

- 2026-08-21：展开面板开始记住用户拖动后的位置。位置通过独立的 X/Y 偏好保存，展开与内容自适应改为优先使用该位置；首次使用仍居中。旧位置落在已断开的屏幕时会夹回当前可用屏幕。呈现转场继续抑制坐标写回，避免 18pt 来源动画被误存为用户位置。新增位置恢复、屏幕失效回收和动画不误写回归；完整 200 项 Swift/视觉测试通过。

- 2026-08-21：运行卡改为实时展示 Codex 公开说明和具体工具动作。commentary 保留段落结构与最多 420 字；命令行展示实际命令并对 Bearer、Token、API Key、Password、Secret 等值脱敏；清爽/详细模式的最近事件上限提升为 4/6 条，每条最多 3 行。

- 2026-08-21：公司额度刷新从任务状态轮询中解耦，改为启动时立即读取、之后每 5 分钟固定刷新；同源请求增加防缓存参数，避免 Edge 返回旧响应。失败继续保留最近一次成功值。

- 2026-08-21：修复胶囊贴到屏幕边缘后展开、回缩会改变位置。根因是新增的 18pt 面板呈现位移动画会触发 `windowDidMove` / `windowDidResize`，控制器误把动画中间帧当成用户拖动结果，覆盖共享锚点。现用带世代号的呈现转场隔离坐标写回：动画期间只改变窗口呈现，不更新持久锚点；旧动画 completion 也不能结束新转场。新增边缘锚点写回抑制、旧转场隔离及完整“边缘胶囊 → 展开 → 回缩”frame 逐点一致回归；测试等待真实转场完成，不再依赖固定睡眠。196 项 Swift/视觉测试（含 20 个胶囊快照）、安装构建、严格签名、单实例、Hermes/自动化诊断与 `git diff --check` 均通过。安装版回读为单个 120×50、layer 3、alpha 1 的真实胶囊。

- 2026-08-21：按 Apple 设计语言收敛主面板，明确排除“减少透明度 / 提高对比度”专项适配。修复公开事件因嵌套纵向栈固有宽度而贴右的问题；事件标签、活动时间线、卡片纵栈与主内容栈改为左对齐并显式等宽，安装版 OCR 回读“正在思考 / 运行命令 / 修改文件”均从左侧统一起读。任务区移到新建任务与额度之前，展开不再抢输入焦点；公司/Codex 额度合并为底部单行；等待/异常/失联卡常驻“继续处理”；项目归属与分组文字提升至 11.5pt；外层去除装饰渐变并减弱描边阴影；胶囊与居中面板之间增加沿来源方向 18pt 的位移提示。新增真实行几何、信息顺序、额度合并、主操作和转场方向回归；安装版已回读目标排版。

- 2026-08-21：修复“等待你”卡片的最终回复只显示首行并在尾部省略。根因是回复标签虽预留 4/8 行高度，却把 `lineBreakMode` 设成了尾部截断；现显式启用多行按词换行，继续保留清爽模式 4 行、详细模式 8 行上限，避免超长回复撑满面板。

- 2026-08-21：修复屏幕居中展开后的持续闪烁并重排卡片文字。录屏逐帧确认根因是居中面板离开原胶囊位置后自动回缩，胶囊恰好在静止鼠标下重现并再次触发 hover，形成循环；现增加 1.2 秒到达面板宽限，回缩后必须真实移出胶囊才允许再次 hover 展开。高频任务列表更新不再使用整组 `easeIn` 淡出和 340ms 位移回弹，避免另一类暗闪。排版改为标题/项目归属/事件/进度/回复五层：历史事件弱化、当前事件强调、步骤数贴合进度条。187 项 Swift/视觉测试（含 20 个胶囊快照）、安装构建、严格签名、诊断与 `git diff --check` 通过；安装版 `--ui-review-panel` 回读到新版布局和当前公开事件。

- 2026-08-21：修复任务卡短内容仍被拉成大块空白。卡片高度继续由运行动态、最终答复和信息密度计算；`WorkItemRowView` 现在直接声明 intrinsic height，加入列表时再以同一高度显式锁定；列表末尾的弹性占位独占所有剩余空间。短内容收缩、长内容按既有 4/8 行上限扩张，窗口恢复到较高尺寸时也不再把空白分摊进卡片。

- 2026-08-20：完成结果改为可在工作岛内直接阅读和回复。此前 rollout 最终答复会被压成 120 字单行，清爽/详细模式仅显示 1/3 行；现最终答复保留段落结构并最多读取 1200 字，清爽模式展示 4 行、详细模式展示 8 行，卡片同步增高，输入提示改为“直接回复…”，发送按钮语义改为“发送回复”。运行中的 commentary 仍沿用 120 字紧凑动态，避免运行卡无谓膨胀。新增最终答复段落保留测试，针对性 13 项测试、安装构建、严格签名、单实例、自动化诊断和 `git diff --check` 通过。全量逻辑测试通过到胶囊视觉快照阶段；快照受当前工作树既有公司额度/录音视觉改动影响而失败，本次未改胶囊也未接受基线。安装版已用 `--ui-review-panel` 重启，但 Computer Use 仍只暴露胶囊窗口，最终结果区的真实视觉回读仍待用户悬停展开或可访问命中后确认。

- 2026-08-20：公司额度统一改为剩余百分比，固定按 $200 = 100% 换算；主面板、迷你胶囊和 Touch Bar 不再显示美元金额，诊断命令仍保留原始金额供排查。

- 2026-08-20：再次修复胶囊收缩/展开错位。根因是两扇窗口互相从当前 frame 推导位置，面板自动改高、跨屏恢复或动画期间可能用隐藏窗口的旧 frame 覆盖新位置。现以独立的可见区域右上角锚点作为单一真值，面板和胶囊只按锚点生成 frame；拖动任一状态时更新锚点。新增自定义 334×364、最大 620×746 和胶囊 6pt 动画画布回归测试；180 项测试、安装构建、严格签名、单实例、自动化诊断与 `git diff --check` 通过。安装版胶囊辅助功能与窗口回读正常；Computer Use 点击边框窗口未真实触发展开，最终真实 hover 展开/回缩的双态坐标仍待用户操作或可访问命中后补验。

- 2026-08-19：修复胶囊和展开面板视觉位置不一致。根因是共享锚点按胶囊 120×50pt 透明动画画布计算，而用户看到的 108×38pt 本体四周内缩 6pt，导致面板相对可见胶囊右上角偏移。现展开、回缩及首次从已保存面板位置生成胶囊时统一以可见本体右上角为锚点；179 项 Swift/视觉测试、安装构建、严格签名、单实例、自动化诊断与 `git diff --check` 通过。安装版黑盒已真实悬停展开并回读新任务入口；其后因当前额度摘要仍为“正在读取…”而未满足旧冒烟文案，故全量 Appium 冒烟未记为通过。

- 2026-08-19：强化录音停止按钮反馈。此前 18pt 红色停止图标点击后无处理中状态，且胶囊要等待最长 5 秒的守护轮询才退出红色波形，容易误判仍在录音。现点击后立即显示“正在结束…”与沙漏、冻结波形并禁用重复点击；完成保存后立即清除录音守护态，失败则恢复录音态并报错。179 项 Swift/视觉测试、安装构建、严格签名、单实例、自动化诊断与 `git diff --check` 通过；真实录音点击观感仍待下一次录音时回读。

- 2026-08-19：修复录音胶囊“停止录音”只暂停、未完成保存。现场辅助功能回读确认按钮已把录音切到“继续”，但旧实现只等待 250ms 就查找“完成”，Catalyst 控件树尚未刷新时会直接失败。现完成按钮在 2 秒内重试，并同时按中英文标签和稳定的 `RecordingView/DoneButton` 标识识别；暂停动作失败也不再静默继续。当前受影响的《0819 低价复盘》已手动点“完成”并在“所有录音”列表回读，时长 25分53秒。179 项测试、安装构建、严格签名、自动化诊断与 `git diff --check` 通过；真实安装版录制 16 秒后从工作岛胶囊点击停止，语音备忘录直接回到“所有录音”，测试录音随后已移入“最近删除”。

- 2026-08-19：修复录音中最小化“语音备忘录”后窗口被周期性恢复。根因有两层：`VoiceMemoGuardian` 每 5 秒遍历最小化窗口的完整辅助功能控件树会触发 Catalyst 窗口回到屏幕；遗留的会议纪要重命名请求还会每 15 秒通过 `openApplication` 再次打开 App。现守护程序先读取窗口的 `AXMinimized`，全部最小化时保留已知录音状态但不遍历控件；重命名链路只操作已运行进程，不再负责启动 App。失效的旧请求已移出运行队列并保留本地备份。178 项 Swift/视觉测试、安装构建、严格签名、单进程、自动化诊断与 `git diff --check` 通过；真实正在录音的窗口最小化后跨过两轮守护轮询仍保持 `kCGWindowIsOnscreen` 缺失，录音进程持续运行。

- 2026-08-19：再次修复“系统深色 + 工作岛强制浅色”时卡片和输入框仍发黑。根因是动态 `NSColor` 在转成图层 `CGColor` 时按系统当前绘图外观解析，而不是按窗口的 `effectiveAppearance` 解析；文字已跟随窗口变浅，图层却仍取到深色值。现所有主面板实体表面与描边在转色时显式使用各自视图的有效外观，并新增跨外观解析回归测试。177 项 Swift 测试、安装构建、严格签名、进程、自动化诊断与 `git diff --check` 通过；真实安装版展开窗口回读为浅灰基底、白色卡片和浅灰输入区，不再半深半浅。

- 2026-08-18：优化工作岛重启后的会话转移等待。目标线程因共享 App Server 仍被其他运行任务占用时，点击“转到 Codex”现在会进入真实待转状态；工作岛轮询到其他任务全部结束后，自动释放遗留服务并打开目标会话，无需用户回来再次点击，同时不会中断仍在绘画或执行的任务。相关 6 项所有权/重启恢复测试、Swift 构建和 `git diff --check` 通过；新版已安装，严格签名、单实例和自动化诊断通过。完整测试中的 20 组胶囊视觉基线受同一工作树既有浅色主题改动影响而不一致，本次未擅自更新；真实“并发任务自然结束后自动跳转”仍待下一次用户触发回读。

- 2026-08-18：录音命名改为“完成后按内容命名”。取消开始录音时添加 `MMdd` 前缀，避免系统把自动前缀误判为用户手动标题；工作岛每 15 秒消费会议纪要服务的重命名请求，按原始标题与录制时间定位语音备忘录，写入模型生成标题并发送 Return 提交，确认列表行已更新后才删除请求。175 项测试、安装构建、严格签名与单实例通过；真实 6 秒测试录音从默认地点标题回读为《内容命名链路验收》，数据库标题与用户编辑标志同步更新。

- 2026-08-18：修复全局录音快捷键未实际添加日期前缀。macOS 26.6 在录音进行中将标题暴露为可写的 `AXStaticText`，旧实现只匹配 `AXTextField`，因此静默跳过；现兼容两种角色，同时继续排除搜索框并要求标题可写。新增角色匹配回归测试，完整 175 项测试通过；安装构建、严格签名、单实例、自动化与 Hermes 诊断通过。真实语音备忘录录音已验证同一辅助功能标题写入链路，录音中与保存后均回读为 `0818 修复验收`；当前 Computer Use 通道不能发送系统级全局热键，快捷键端到端仍需下一次用户实际触发回读。

- 2026-08-18：修复工作岛重启后从“等待你”任务跳转 Codex 时出现“已在另一个应用中打开”。根因是恢复连接收到 `thread/unsubscribe` 回执后，原独立 App Server 仍可能保留已加载线程；当目标是最后一个工作岛任务时，现会在深链跳转前停止遗留服务，确保 Codex 能重新取得线程。若重启后仍有其他工作岛任务运行，则不冒险停止共享服务，并在卡片内提示等待。辅助功能回读同时把“已在另一个应用中打开”识别为真实跳转失败，不再误报成功。完整 173 项测试、`git diff --check`、安装构建、严格签名、单实例、自动化与 Hermes 诊断通过；未替用户点击当前待读任务，因此该真实会话的最终接管仍待用户下一次点击回读。

- 2026-08-18：全局录音快捷键开始录制后，会将语音备忘录当前录音标题加上当日 `MMdd` 前缀，例如 8 月 18 日为 `0818 新录音`；若标题已带当天前缀则不重复添加。停止并保留逻辑不变。完整 172 项测试通过，安装构建、严格签名、单实例、自动化与 Hermes 诊断通过；为避免生成无效会议录音，本次未主动触发真实录音，标题写入待下一次实际快捷键录音回读。

- 2026-08-17：全局录音快捷键 `⌥⌘R` 改为按真实录音状态切换动作：未录音时开始语音备忘录，`VoiceMemoGuardian` 检测到录音中时结束并保留；菜单中的同一快捷键文案同步切换为“停止录音并保留”。新增空闲/录音两种决策测试，完整 Swift 与视觉回归测试 171 项通过；新版本已安装，严格签名、单实例、自动化与 Hermes 诊断通过。

- 2026-08-17：修复浅色外观“半深半浅、文字看不清”。根因有两层：外层仍使用深色 HUD 材质、黑色渐变和深重阴影；任务卡与状态胶囊把自适应 `NSColor` 转成 `CGColor` 后未在外观变化时重算，导致切到浅色后残留旧深色图层。现浅色改用 `underWindowBackground`、轻量浅色渐变与阴影，任务卡、输入区和状态胶囊均在 `viewDidChangeEffectiveAppearance` 时刷新，并为浅色卡片增加独立悬停表面。Swift 与视觉回归测试 169 项通过；新版本已安装，严格签名、单实例和自动化诊断通过，安装版真实展开截图回读为统一浅灰基底、白色卡片和清晰深色文字。

- 2026-08-15：公开 README 完成产品推广重构。默认 `README.md` 改为英文主入口，新增 `README.zh-CN.md` 中文入口；第一屏只保留“Codex 在后台工作，需要你时再回来”的核心定位。重新录制 12 秒完整桌面演示，以实际屏幕比例展示右上角胶囊的位置和展开效果；发布 `docs/media/ai-work-island-fullscreen-demo.webp`，并提供 1080p MP4。公开素材对桌面背景做强模糊，只保留工作岛区域清晰，同时遮挡公司额度，原始录屏不进入仓库。安装章节明确当前为免费 ad-hoc 预览版、尚未 Developer ID 签名或公证，并保留系统设置“仍要打开”、只从本仓库下载及校验 Release 的真实边界。公司额度、会议纪要、录音与 Touch Bar 下移为可选扩展，不再参与第一印象竞争。此次只修改文档与演示素材，未改动正在进行的 AppDelegate、会话桥和 UI 测试代码。

- 2026-08-14：将工作岛自建 Codex 会话从“等待整个共享 App Server 空闲”改为线程级所有权转移。根因是旧实现把共享服务生命周期误当作线程所有权：目标线程只有在其他任务全部结束后才退订，并且退订仅固定等待 250ms、不验证回执。现每个进程内线程显式经历 `workIsland → transferring → codex`；点击后立即只向目标连接发送 `thread/unsubscribe`，收到确认才按稳定 thread ID 打开 Codex，其他线程和共享服务继续运行；转移期间阻止重复点击与卡片发指令，失败在卡片显示原因，超时通过同一连接 `thread/resume` 确认恢复后回滚。工作岛重启后若内存连接已丢失，会通过仍在运行的共享 socket 建立恢复连接并退订目标线程。共享服务仅在注册表与调用计数都为空时关闭。本机 Codex 生成的最新实验协议 schema 仅提供 `thread/unsubscribe`、`thread/loaded/list`，没有 `thread/unload`，因此退订确认是客户端所有权交接边界，进程关闭不再参与单线程转移。新增多线程隔离、失败/超时回滚、完成竞态幂等、重启缺失内存状态、最后线程关闭服务等状态机测试。完整 169 项 Swift/视觉测试、`git diff --check`、安装构建、严格签名、单实例和 Hermes/自动化诊断通过。真实安装版同时创建 A（90 秒）与 B（45 秒）两条工作岛任务；B 完成时 A 回读仍在 60/90 秒，点击 B 后它立即从工作岛待读中移除并按稳定 ID 交给 Codex，A 随后继续到 `TRANSFER_PEER_OK`，未被中断；A 完成后共享 App Server launchd job/PID 自动消失。

- 2026-08-14：修复工作岛自建任务完成后整卡点击无反应。根因是 `openWorkItem` 对 `.waiting` 先走通用问题详情，未先识别 Codex 会话；同时仍有其他工作岛任务运行时，所有权转移反馈只显示在菜单栏，卡片看起来没有响应。现 Codex 卡片优先进入统一会话转移状态机，其他任务未结束时在卡片内持续显示“结束后自动转到 Codex”，可转移后显示“正在释放工作岛占用…”。新增等待态 Codex 卡与失败自动化主动作优先级断言，完整 162 项 Swift/视觉测试、`git diff --check`、安装构建和严格签名通过；安装版回读到目标等待卡与其他三条运行任务，未中断现有任务。

- 2026-08-14：修复外接显示器、分辨率或 Space 拓扑变化后工作岛主面板与胶囊可能同时不可见。根因是窗口只在首次收缩时校正屏幕位置，未监听 `NSApplication.didChangeScreenParametersNotification`；显示器重配置后 AppKit 可能保留已断开屏幕坐标或丢失非激活浮窗的排序。现按用户真实可见请求恢复主面板或胶囊，重新应用浮动层级、跨 Space 属性和可见屏幕约束；用户主动隐藏时不误弹出。新增“恢复前置窗口”和“隐藏状态不恢复”回归断言，完整 161 项 Swift/视觉测试、`git diff --check`、安装构建、严格签名、单进程及 Hermes/自动化诊断通过；安装版辅助功能与截图回读到可见的“公司 $172”胶囊。

- 2026-08-14：修复悬浮胶囊“公司 $172”显示为“公司…”。根因是录音停止按钮隐藏后，其前置约束仍为普通状态的额度文字预留宽度；现普通状态将文字延伸到胶囊右侧，仅录音时为停止按钮让位，不扩大 108×38pt 胶囊，也不改变悬停与录音交互。新增额度文字可用宽度回归断言，完整 159 项 Swift/视觉测试、`git diff --check`、安装构建、严格签名、单进程及 Hermes/自动化诊断通过。安装版当前有两条“等待你”任务，按既有优先级规则锁定任务状态，因此未在安装版轮播中直接回读公司额度页。

- 2026-08-14：修复工作岛新建会话运行期间偶发卡片消失。根因是 `thread/start` 成功后，界面立即完全依赖 Codex SQLite/rollout 索引；索引在活跃态切换或尚未导入时短暂缺少该 thread，整轮刷新会移除卡片。现由 `CreatedThreadContinuity` 在拿到稳定 thread ID 后保留最长 30 分钟的活跃兜底；扫描明确读到完成/待读后交还权威索引，过期则自动清理，避免幽灵任务。新增“首次索引空窗、活跃后再次空窗、完成交接、超时清理”回归断言，完整 158 项 Swift/视觉测试通过；安装版已覆盖，严格签名、单进程和 Codex 诊断回读通过。

- 2026-08-14：修复对待读会话打勾“标记已阅”后工作岛自动缩回。根因是卡片移除动画缩短窗口，指针校验将窗口从鼠标下移走误判为用户主动移出；现布局变化后保持展开，直到鼠标重新进入再真实移出才恢复 0.5 秒自动回缩。新增状态机断言，完整 156 项 Swift 测试通过。因验证时语音备忘录正在录音，未覆盖或重启安装版，待录音结束后再做安装版黑盒回归。

- 2026-08-14：修复浅色外观下详细模式的运行文案不可见。此前会话详情写死为半透明白色，在白色任务卡上失去对比度；现统一使用随 Aqua/Dark Aqua 自动解析的 `labelColor`，清爽模式继续使用语义次级色，忙碌态保留强调色。新增文字颜色回归断言。

- 2026-08-14：修复“新任务”输入框纯文本 `Command-V` 在浮动面板首次激活或没有活动 field editor 时被 AppKit 默认事件吞掉。`PastedImageTextField` 现优先读取剪贴板纯文本并替换当前选区；没有 field editor 时直接追加到字段，图片粘贴与退格删除图片逻辑保持不变。新增无 field editor 的纯文本粘贴回归测试。

- 2026-08-14：新增四项工作岛能力。公司额度最近一次成功值使用 `CompanyModelQuota` Codable 快照持久化，应用重启立即可用；Edge 公司平台标签页休眠导致读取失败时，后台刷新原标签页并延迟重试，不切换当前标签。录音胶囊新增红色停止按钮，点击后复用“结束并保留”链路，录音期间悬停不再抢先展开面板。菜单新增持久化“静默运行（开会 / 投屏）”，后台执行与刷新不变，仅抑制任务完成自动展开和录音静音通知/声音。外观新增“跟随系统 / 浅色 / 深色”，面板、卡片、输入区、描边和胶囊颜色按外观解析。新增持久化、停止录音和外观回归测试；20 组胶囊视觉基线已更新，完整 155 项 Swift 测试通过。

- 2026-08-14：安装并初始化官方 Xcode 26.6（build 17F113），`xcode-select` 已切换到 `/Applications/Xcode.app/Contents/Developer`；安装 Appium 3.6.0 与 Mac2 driver 4.2.0，Doctor 的必需检查全部通过。完整工具链发现并修复五处 AppKit 动画 completion 的 Swift 6.3 主线程隔离警告，并将烟灰色板测试从脆弱的浮点完全相等改为严格容差比较。保留“Automation Mode 每次认证”的系统安全默认值，未执行降低认证级别的可选命令。新增 `Tests/E2E/appium_work_island_smoke.mjs` 与 `script/run_appium_smoke.sh`，不依赖第三方测试客户端，直接通过 W3C WebDriver 对安装版执行 10 项断言：Bundle ID 绑定、胶囊读取、真实悬停展开、新任务/额度入口回读、输入写入与清理、离开回缩、三轮竞态重放及截图回读。完整八步发布候选闸门实跑通过：152 项 Swift/视觉测试、补丁格式、构建安装、严格签名、单实例、数据源诊断、辅助功能树和 Appium 黑盒冒烟均通过。

- 2026-08-14：建立第一阶段测试审查体系。新增 20 个不依赖 XCTest 或外部包的 AppKit PNG 视觉基线，覆盖空闲、运行、排队、等待、失败、失联、完成、公司额度、Codex 5 小时/周额度三级阈值及录音/静音胶囊；完整测试增至 152 项。新增 `script/verify_release_candidate.sh`，统一执行 Swift/视觉测试、`git diff --check`、构建安装、严格签名、单实例、Hermes/自动化诊断与安装版辅助功能树；本轮完整闸门通过。`docs/TESTING.md` 固化 15 条安装版黑盒发布清单。Computer Use 回读正常启动胶囊与 `--ui-review-panel` 展开面板，验证新任务输入可写入并已清空临时文本；两条运行卡、额度、详细模式布局和辅助功能入口完整，未发现新的功能故障。后续已安装完整 Xcode 并接入 Appium Mac2 冒烟；完整 15 条清单仍未全部自动化。

- 2026-08-14：完成首轮克制视觉优化，并将发布候选版本更新为 `0.5.18` build 50、正式标签统一为 `v0.5.18`。外层面板继续使用玻璃材质，任务卡与新任务/续接输入框改为高不透明实体表面，减少重复嵌套玻璃；提高标题、路径、摘要、输入提示和页脚的字号或对比度，悬停统一为轻微提亮。清爽 Codex 卡从 116pt/两行摘要压缩为 104pt/单行，详细卡从 148pt/四行压缩为 136pt/三行，任务逻辑、372pt 默认宽度、悬停操作、会话绑定和状态分组不变。151 项 Swift 测试、`git diff --check`、构建安装、严格签名、单进程及 Hermes/自动化诊断通过；解压包版本、标签与签名回读通过，ZIP 的 SHA-256 为 `11acc0fe3ab3ddfd7b55fd4f69ba2a5d2138f65ed2babf14d8ab1717a6edd537`。折叠态与展开态截图已回读，安装版胶囊显示橙色“1 等待你”。

- 2026-08-13：修复安装或菜单重启后前台窗口每 10 秒被工作岛/ChatGPT 抢走。根因是 `launchctl submit` 创建的临时重启任务退出后会被 launchd 节流重启，持续执行 `open`；重启器现打开安装版后主动注销自身，构建脚本与应用内重启共用该一次性规则。已清除本机遗留的 `dev.kanyun.AIWorkIsland.Relauncher` 服务；151 项 Swift 测试通过。
- 2026-08-13：修复没有任务完成时工作岛仍会快速展开再缩回。根因是任意第二次 LaunchServices 启动都会发送 `restoreDashboard` 分布式通知，常驻实例收到后调用 `showAutoCollapsing()`；同时旧单实例策略用 PID 数字大小判断新旧，PID 回绕后会保留重复实例。现移除“重复启动即展开”链路，显式拒绝 AppKit 的后台 reopen 窗口恢复；胶囊悬浮/点击、菜单操作和真实任务完成提醒维持原行为。新增内核持有的 `app-instance.lock`，并声明 `LSMultipleInstancesProhibited`，不再依赖 PID 顺序。151 项 Swift 测试、`git diff --check`、构建安装、严格签名、自动化诊断通过；安装版连续执行 3 次 `open -n` 后全程保持 120×50pt 胶囊，进程数始终为 1。

- 2026-08-13：修复同一会话从不同入口打开时绕过占用释放。此前只有任务卡悬停按钮“转到 Codex”走工作岛释放状态机，点击整张 Codex 卡片和 Touch Bar 的处理中/待读仍直接发送深链；当共享 App Server 还在执行另一条工作岛任务时，会同时触发“未找到对话”和“此任务正在其他位置运行”。现所有用户打开入口先按本地工作岛 thread 注册表统一判定：工作岛会话全部走同一转移/等待链路，普通 Codex 会话才直接打开。完整 152 项 Swift 测试、安装构建、严格签名、单进程、自动化诊断和 `git diff --check` 通过；安装版二进制回读包含统一入口方法。真实并发任务自然完成后，共享 App Server launchd job 与 PID 均自动消失。Computer Use 可回读安装版胶囊，但受 0.5 秒自动回缩和 Codex 应用安全限制影响，未自动重放“并发中点击任务卡”的完整 UI 场景。

- 2026-08-13：修复再次打开应用或从菜单重新显示后面板永久不回缩。根因是这些入口直接调用 `show()`，将 `isHoverExpanded` 重置为 `false`，绕过胶囊点击/悬停共用的自动回缩生命周期；切换面板显示方式也会额外触发同一路径。现新增 `showAutoCollapsing()`，显式打开与菜单显示统一进入真实指针校正和 0.5 秒回缩，切换显示方式只调整层级、不再强制展开。完整 151 项 Swift 测试、安装构建、严格签名、单进程、自动化诊断和 `git diff --check` 通过；安装版第二次启动后窗口先展开为 327×524pt，鼠标位于面板外时 0.8 秒后真实回读为 120×50pt 胶囊。

- 2026-08-13：修复工作岛会话完成后点击“转到 Codex”出现“未找到对话 / 此任务正在其他位置运行”。根因是共享独立 App Server 即使断开 WebSocket 并 `thread/unsubscribe`，仍会在进程内保持 thread 已加载；Codex 因此判定会话仍属于另一个应用。现所有 App Server 调用纳入并发计数，完成或主动转移先退订，最后一个工作岛任务结束后自动关闭共享服务；界面中途重启留下的 launchd 服务也会在安全转移时清理；仍有并发任务时不误停、不打开报错页，改为服务空闲后自动跳转。完整 150 项 Swift 测试、安装构建、严格签名和 `git diff --check` 通过；真实新建 `IDLE_RELEASE_OK` 会话完成后 launchd 服务/PID 自动消失，从工作岛点击后 Codex 回读到目标标题与结果，未出现两类错误；原故障会话也已重新打开。

- 2026-08-13：新增工作岛自更新/重启保护。工作岛新建会话改由 launchd 托管的独立 Codex App Server 执行，界面通过 Unix Socket WebSocket 连接；工作岛退出只断开界面连接，不再终止已经接受的 turn。菜单“重启应用”和 `build_and_run.sh` 均由 launchd 独立拉起新版，验证模式要求 PID 真实变化后才通过。149 项 Swift 测试通过；真实端到端测试中，15 秒任务执行期间工作岛从 PID 67339 重启为 70583，App Server 始终保持 PID 64414，任务最终正常返回 `DURABLE_OK`。安装版严格签名与自动化诊断通过。

- 2026-08-13：修复 Touch Bar 额度圆环显示不完整。根因是 `QuotaRingView` 为调整进度起点旋转了整个 112×30 图层，左侧 24×24 圆环随图层绕组件中心偏移并被胶囊裁切；现直接绘制从顶部开始的圆弧，图层保持原位。新增路径边界与无变换回归检查，完整 147 项 Swift 测试、`git diff --check`、安装包严格签名通过；实际 Touch Bar 截图回读确认 Codex“周 86%”圆环完整闭合。构建脚本启动探针曾因既有进程退出延迟误报，但安装版随后保持运行并被真实 Touch Bar 截图验证。

- 2026-08-13：修复“工作岛新建会话出结果后，点击卡片只唤起 Codex 但不进入目标会话”。根因是导航改为只搜当前已渲染的侧栏标题：新 thread 已进入持久索引但侧栏未渲染时永远匹配不到，同名会话也无法区分。现恢复按 thread ID 的 `codex://threads/<id>` 定向跳转。实际端到端回测从已安装的工作岛展开面板，点击真实“测试”卡片的打开按钮，Codex 当即显示该会话及结果“收到。”；工作岛待处理数从 2 降为 1，`viewed-thread-ids.json` 持久写入被点击的 `019ffa6c-f444-7a93-a016-ca508c355016`。同时修正失真的 AX 成功判据：当前版本即使已正确跳转，辅助功能树也可能不暴露正文和侧栏。

- 2026-08-13：修复“已进入 Codex 但工作岛仍显示等待你”。根因是 `codex://threads/<id>` 已实际打开会话后，工作岛仍以辅助功能树是否出现正文标题为成功条件；该标题未及时暴露时会撤销乐观已阅状态。现重新以 Codex 侧栏真实任务行的选中作为成功条件（最长等待约 8 秒），成功后才持久标记已阅；不会因会话已打开但正文标题未暴露而恢复等待。

- 2026-08-13：修复面板离开后偶发永久不回缩。根因是延迟回缩任务在状态瞬时不满足时直接返回，却保留 `pendingAutoCollapse`；后续真实离开会被误判为已排队，无法再次建立回缩任务。现以请求序号隔离过期回调，并在当前延迟任务执行时先清除待处理标记，确保坐标校正后的后续离开可以重新排队。147 项 Swift 测试、安装构建、严格签名、启动进程和 `git diff --check` 通过；当前运行安装版。

- 2026-08-13：修复面板偶发不自动回缩。根因是无边框浮窗在焦点/响应者切换时可能漏发 `mouseExited`，使内部状态永久停在“鼠标仍在面板内”，回缩计时器无法建立。悬停展开期间现每 0.1 秒按实际鼠标坐标校正悬停状态；离开后只会排队一次 0.5 秒回缩，重新进入仍会取消队列。147 项 Swift 测试、`git diff --check`、安装构建和严格签名通过；构建脚本的启动探针被本轮中断，未将它作为通过项，但自动化诊断与真实胶囊回读正常。

- 2026-08-13：完成安装版基础 UI 交互验收的可执行部分：严格签名、进程、自动化诊断通过；真实展开面板回读到新任务工作目录/输入/发送、额度、待处理、运行任务和卡片续接输入，点击新任务输入框也确认可聚焦。胶囊点击后约 0.5 秒会按设计回缩，Computer Use 无指针移动能力，不能在可观测窗口内直接验收离开回缩；随后关闭面板的 Computer Use 请求连续超时，但应用进程、签名与诊断仍正常。界面状态源显示会议纪要任务为 `stale`，未修改该真实状态。
- 2026-08-13：补充基础 UI 行为回归覆盖：信息密度模式的卡片尺寸、任务较多时的面板高度上限，以及异常任务优先固定显示并保留无障碍说明。完整 147 项 Swift 测试与 `git diff --check` 通过；未重装应用，因为本次仅改动测试。
- 2026-08-13：修复工作岛新建任务偶发降为受限权限。真实状态库曾出现工作岛会话被持久化为 `managed + on-request`；现除 `thread/start` 的 `danger-full-access + never` 外，首个 `turn/start` 也显式写入可持续到后续轮次的 `dangerFullAccess + never`，避免宿主权限配置在建线程后覆盖。新增协议参数回归测试；完整 145 项测试、安装构建、严格签名、单进程和 `git diff --check` 通过。
- 2026-08-13：修复“等待你”自动展开/回缩与录音胶囊叠加后，屏幕只剩胶囊但内部仍误判主面板已展开，导致点击无响应。胶囊激活现以实际窗口可见性自愈：胶囊可见且主面板不可见时仍允许恢复；新增状态不同步回归测试，完整 145 项测试通过。
- 2026-08-13：增强会话完成提醒：当已在胶囊态的任务从运行/排队真实进入 Codex 待读或自动化已完成时，主面板自动展开并至少保留 4 秒；回缩后继续沿用持久“等待你”标记，直到打开或标记已阅。首次启动读取历史完成项不会误触发，也未增加系统通知，避免与 ChatGPT 通知重复。新增状态转换与 4 秒保持回归测试，完整 144 项测试、安装构建、严格签名、单进程与自动化/Hermes 诊断通过；真实 UI 捕捉时用户正在录音，Computer Use 只能回读录音胶囊而无法可靠读取第二个无边框面板，因此本轮不宣称完成展开的视觉验收，临时自动化测试状态已删除。
- 2026-08-13：重构 GitHub README 的产品信息层级：补齐价值说明、三步工作流、能力分区、隐私表格、普通用户/源码安装路径和最小自动化 JSON 示例；从已有真实界面演示中拆出“胶囊展开”和“任务中枢”两个轻量 GIF，与总览动画组成三处动态展示。仅修改文档与 `docs/media`，未触碰当前未提交的产品代码。
- 2026-08-13：录音态胶囊新增七柱实时波形。复用现有 AVAudioEngine mean/peak 计算，将约 10Hz 的归一化音量更新到 7 个固定 CALayer；不保留原始采样，停止录音立即清空。143 项测试、安装构建、严格签名和单进程通过；真实进行中的语音备忘录回读到红色动态波形与“录音 40分”，胶囊尺寸不变且未操作用户录音。稳定运行约 39 秒时整个工作岛进程 RSS 约 147MB、CPU 约 3.7%，这是应用全量占用，不能视为波形增量；波形自身仅 7 个图层和单个 Double 音量值。`--ui-review-recording-waveform` 仅用于无真实录音时做视觉验收。
- 2026-08-13：修复面板启动后一直停在“正在读取…”且不显示会话。根因是首次快照串行等待 Edge 公司额度读取，ScriptingBridge 偶发阻塞时连带阻止会话渲染；现改为 Codex/自动化快照先立即应用，公司额度在独立任务中随后更新，额度阻塞不再影响内容。结合录音波形变更后完整 143 项测试通过，真实界面回读用于验收。
- 2026-08-13：修复已阅会话循环恢复“等待你”。Codex 打开会话会更新 SQLite 的会话访问时间，旧逻辑误把它当成新结果；现在索引扫描只用 rollout 内容更新时间判断是否产生新结果，打开或聚焦会话本身不会复活未读，真正的新输出仍会恢复提醒。新增索引数据库回归测试，完整 140 项测试通过。
- 2026-08-13：新增清爽/详细双模式并细化详细模式可读性。菜单切换立即重排并持久化；详细模式扩大摘要和卡片空间、提升字号与对比度、分离进度/当前/最新，去除正文蓝色和整卡蓝色悬停。139 项测试通过，安装构建与严格签名通过；真实安装版回读到等待卡多行结果、运行卡三段摘要及完整输入区，未出现遮挡。`--ui-review-panel` 仅用于保持面板展开做视觉验收。
- 2026-08-13：统一待读卡片的主要点击目标。标题、状态、摘要和卡片主体继续直接打开 Codex；“继续该项目”输入为空时，右侧圆形上箭头也打开 Codex，输入文字或图片后仍负责发送。该规则只用于“等待你”，运行中卡片的空发送按钮不跳转，避免误释放后台任务。137 项测试、安装构建、严格签名和单进程通过。
- 2026-08-13：进一步收紧“等待你”点击时序。用户点击待读会话时，桌面面板、胶囊、Touch Bar 和菜单计数先立即隐藏当前提醒；Codex 会话验证成功后再持久化已阅，若跳转失败则撤销临时状态并自动恢复提醒，不再出现 Codex 窗口已弹出而工作岛仍等待约 3 秒的错位。公司额度的进程定向扫描同时移除不存在的 Edge `url` KVC 回退，避免新标签加载瞬间因空 URL 崩溃。合并 GitHub 更新功能后 136 项测试、安装构建、严格签名、单进程通过；安装版额度回读 86%、$171.92。
- 2026-08-13：新增 GitHub Release 更新提醒。启动自动检查带 24 小时缓存，菜单可手动检查；仅提示正式 Release，支持跳转下载、稍后提醒和跳过版本。兼容当前 `v2026.08.13` 日期标签，避免把现有安装误判为旧版；后续 `build_release.sh` 默认使用 `v<应用版本>`。136 项测试、Production 构建、临时 App 严格签名和版本元数据回读通过；未覆盖 `/Applications`。

- 2026-08-13：修复同时运行常规 Edge 与直播监控/截图无头 Edge 时公司额度始终不可用。原 AppleScript 按 Bundle ID 连接到后台无头实例，真实已登录的两个 Model 标签位于常规 Edge PID 48314；现按进程筛选 `.regular` Edge，再用 ScriptingBridge 定向该 PID 执行同源 `/api/v1/users/self` 请求。保留直播监控进程且不读取 Cookie。安装版诊断回读公司额度剩余 86%、$171.92，132 项测试、安装构建、严格签名和单进程通过。
- 2026-08-13：修复打开 Codex 会话后“等待你”不能立即消失。会话成功打开并持久化查看时间后，桌面面板、迷你胶囊、Touch Bar 和菜单计数立即按“线程 ID + 查看时间”隐藏当前旧结果；若同一线程随后产生更新时间更晚的新结果，提醒会正常恢复。刷新已在途时不再丢弃新请求，而是在当前扫描完成后自动补刷，旧快照也不会把刚清除的提醒带回来。132 项测试、安装构建、严格签名、单进程、rollout 与自动化诊断通过。
- 2026-08-13：桌面工作岛首次展示时默认展开“正在执行”；“等待你”仍固定置顶并始终展开，用户手动折叠后的选择不被轮询覆盖。面板内分组按钮、任务操作按钮、发送按钮、目录按钮和文本输入框统一接受非激活窗口首击，使第一次点击同时激活窗口并触发目标操作。130 项测试通过，安装构建与严格签名通过；安装脚本的 `pgrep -x` 在当前 locale 下报 `illegal byte sequence` 导致启动检查假失败，随后从真实进程日志、安装包签名和安装版自动化诊断回读确认应用已运行。
- 2026-08-13：面向无 Codex 账号的公司同事调整额度默认逻辑。桌面面板、Touch Bar 和迷你胶囊均优先展示公司额度；公司额度缺失时明确显示“待配置 · Edge 登录”，Codex 额度仅在本机读到有效数据时追加显示。README 补充每位用户独立登录公司模型平台的配置步骤。
- 2026-08-13：发布准备验证 127 项测试、安装构建与严格签名通过；README 增加面向非开发者的 GitHub Release 安装和首次授权说明。
- 2026-08-13：用户确认跳转卡在“搜索聊天”弹窗。实查该弹窗的焦点控件为 Electron `AXComboBox`，占位为“命令菜单”，不是稳定的聊天搜索文本框；辅助功能设值不产生可靠 React 输入事件，键盘输入又可能落入当前会话正文，因此彻底移除搜索输入自动化。当前 Codex 版本实机验证 `codex://threads/<thread-id>` 已能稳定打开主窗口目标会话且未出现旧版隐藏 overlay 错误；正式路径改为先在检测到 `AXComboBox` 时发送 Escape 关闭卡住的命令菜单，再按稳定 thread ID 直达，并等待目标正文出现、同时拒绝“Conversation state not found/未找到对话”后才标记已阅。安装版用 `019ff918-f2eb-73a3-a570-98baa2de51f8 / 测试新会话` 回读 `Open thread ID success`。
- 2026-08-13：用户回报首次搜索修复后点击仍无反应，进一步定位到卡片事件层：`WorkItemRowView` 虽有整卡 `mouseUp`，但标题、状态、进度摘要等 AppKit 子视图会截获鼠标且事件不向父视图冒泡，导致视觉主体并非真实点击区。现为状态点、标题/详情、状态文字和会话摘要绑定统一 `selectRow`，输入框及操作按钮保持独立。新增 `--diagnose-open-thread <title>` 安装版诊断，真实回读“测试新会话”为 `success`。126 项测试、构建安装、严格签名与 `git diff --check` 通过。
- 2026-08-13：修复工作岛新建会话首次点击无法进入 Codex。原实现只等待目标标题出现在当前可见侧栏；当新会话已写入 Codex 持久索引、但所属文件夹尚未加载到侧栏时，等待不会产生进展。现在先尝试侧栏直达，未命中则按压 Codex 辅助功能树中真实可见的“搜索”按钮，且必须回读到搜索输入框后才写入标题，再按精确结果进入，避免标题误发到当前会话。125 项测试、构建安装及签名通过；目标测试线程 `019ff918-f2eb-73a3-a570-98baa2de51f8` 已由 Codex 内部导航按 ID 成功打开。Computer Use 仍因安全限制不能直接操作 `com.openai.codex`，所以搜索弹层的端到端鼠标回放未自动执行。

- 2026-08-13：0.5.17 build 49 完成工作岛任务处理链路收敛。首次转到 Codex 不再使用会广播到隐藏窗口的深链，改为最长等待 8 秒直至真实侧栏任务可按压；未同步时保留任务并提示重试，不误标已阅。`等待你` 卡片新增单条“标记已阅”，分组新增“全部已阅”。Codex 两行摘要抽为 `CodexCardStatusSummary`：运行态使用“当前/最新”，待读态使用“需要你/结果”，自动去除“正在/刚刚”等重复前缀。鼠标移入展开、移出回缩保持不变。更新前基线已提交为 `c272be1` 并打标签 `rollback-before-work-island-ux-20260813`，安装版和完整 Git bundle 位于 `/Users/kanyun/Documents/Codex-Hermes-Touch-Bar-backups/20260813-before-ux/`。125 项测试、构建安装、严格签名、单进程、自动化诊断和 `git diff --check` 通过；安装版胶囊回读为“3 等待你”。Computer Use 当前无法对该无障碍窗口执行指针移动，未实际触发“全部已阅”，避免清除用户现有待读状态。
- 2026-08-12：真实新建“工作岛首次跳转测试：只回复 OK”，安装版卡片回读到 `等待你 / 上轮：OK`，证明创建、执行和持久化正常；但首次跳转验收未通过。该新线程尚未进入 Codex 可见侧栏，侧栏直达返回 `threadNotVisible`，正式路径仍会回退深链，因此“未找到对话”假错误尚未被完整消除。尝试的搜索聊天路径因无法确认搜索输入框而撤回，未留半工作实现。当前保留侧栏已可见时直达、不可见时深链兜底。
- 2026-08-12：Codex 任务卡的运行状态摘要由单行扩为两行。第一行展示明确计划进度与当前动作，第二行展示最近动态；完整文本仍保留在悬浮提示中。卡片高度增加 14pt，面板总高度同步按每张 Codex 卡增加 54pt，宽度不变。121 项测试、构建安装、严格签名、单进程及自动化诊断通过；安装版胶囊状态回读正常。由于面板在界面抓取等待期间按设计自动回缩，本轮未取得稳定的展开面板截图，需在后续可持续悬停的 UI 自动化环境补做视觉截图。
- 2026-08-12：针对工作岛新任务首次“转到 Codex”时正文已打开却仍弹“未找到对话”的假错误，跳转现在先激活 Codex，并在目标标题已进入可见侧栏时通过辅助功能直接按任务行打开；只有侧栏尚未加载或辅助功能不可用时才回退会广播给隐藏窗口的 `codex://threads/<id>`，同时保留点击兜底。实机回读确认目标工作岛任务已存在于 Codex 侧栏辅助功能树；121 项测试、安装构建、严格签名和单进程通过。需要下一次真实新建任务首次跳转确认不再进入深链兜底。
- 2026-08-12：工作岛“转到 Codex”以可用性为优先，保留 `codex://threads/<id>` 的直接跳转。曾尝试以 `searchChats` 的 UI 自动化替代它来避开隐藏 avatar overlay 的“未找到对话”提示，但历史任务未加载在侧栏时没有可验证成功回执，实际会吞掉点击，因此已完整回退。实查目标线程和标题均存在于 Codex SQLite；120 项测试、构建安装、严格签名、单进程和 `git diff --check` 通过。现点击会和原版一样直接打开对应会话，残留提示仍是 Codex 桌面端自身的隐藏 overlay 问题，未能在工作岛侧可靠消除。
- 2026-08-12：修复从工作岛新建任务总是索要权限。根因是 `thread/start` 未显式携带权限配置，App Server 回退为向用户审批；工作岛新线程现固定传 `approvalPolicy=never` 与 `sandbox=danger-full-access`，与用户当前 Codex 完全访问模式一致。已有线程不回写。
- 2026-08-12：0.5.16 build 48 新增 Codex 运行进度展开界面，从 rollout 增量读取 commentary 与 `update_plan`，展示当前/最近活动及明确步骤计数；新任务开始时重置旧进度，不虚构百分比。兼容旧 `function_call` 与当前桌面版 `custom_tool_call/exec` 两种计划事件，以及当前桌面日志中带引号的 `status` 字段。120 项测试、构建安装、严格签名、进程、rollout/自动化诊断和安装版辅助功能树回读通过；真实卡片回读到“正在”及两条“刚刚”动态，诊断回读明确计划为 `4/4`。
- 2026-08-12：新增任务卡“转到 Codex”，解决工作岛持有 App Server 时 Codex 只能提示“任务在其他位置运行”且看不到阶段进度的问题。实查旧安装版主进程下有 4 个独立 `codex app-server --stdio`，已持续约 13–58 分钟；安装新版后旧占用全部退出。113 项测试、构建安装、严格签名、自动化诊断和 `git diff --check` 通过；安装版辅助功能树回读到按钮描述“停止工作岛占用并转到 Codex”。四个旧任务并非完全未执行，均已有 commentary，其中两个已推进到文件变更请求；因安装重启被标记为 `interrupted`，现在可逐项用“转到 Codex”打开后继续。
- 2026-08-12：修复胶囊点击展开后不再自动回缩，以及打开 ChatGPT/Codex 会话后任务仍永久显示“等待中”。点击和悬停现在统一进入可自动回缩生命周期；工作岛不再把自建 thread ID 永久等同于未读，而是按本地查看时间与会话结果更新时间比较，新结果仍会重新提醒。新增两项保留测试，113 项测试通过。
- 2026-08-12：修复工作岛新会话完成后消失。根因有两层：CLI 创建的线程来源不符合 Codex 桌面显示口径；即使改为 App Server，完成线程也会因未读集合缺失被工作岛过滤。新建改为持久 `vscode`/`codex-desktop` 线程，并维护工作岛 thread ID 注册表。安装版真实提交“工作岛完成后保留测试”，rollout 回读 `task_complete / 已保留`，SQLite 回读 `source=vscode`、`thread_source=vscode`、`archived=0`、preview 非空，完成后工作岛仍显示为“等待你”。111 项测试通过。
- 2026-08-12：完成安装版图片会话闭环。首次真实回读发现图片可粘贴并显示 `图×1`，但 field editor 吞掉 Backspace/Delete；现由两个输入框 delegate 转发 `deleteBackward:`/`deleteForward:`，安装版确认 `图×1` 随 Backspace 消失。首次带图发送又发现 `codex exec --image` 的可变参数会吞掉后续文字，现以 `--` 终止图片参数。最终真实窗口提交后输入栏立即恢复、运行数从 1 增至 2，rollout 同时读回 `input_image`、`local_images`、文字指令和图片识别回复；后台进程退出且临时 PNG 已清理。111 项测试、构建安装、严格签名和 `git diff --check` 通过。
- 2026-08-12：修复“新任务”长期停在“正在创建”并连带禁用图片输入。根因是独立 `codex app-server --stdio` 与已运行的 Codex 桌面端发生 active-writer 竞争，可能在 `thread/start` 前无限等待；新会话现改由官方 `codex exec --json -C <目录> --image <图片>` 创建持久会话，进程启动后立即恢复输入栏，后台排空输出并在结束时清理临时图片。既有卡片续接仍走桌面 IPC。安装版真实窗口提交“只回复 OK”后约 1 秒输入框恢复、运行任务由 1 增至 2，并出现 Documents 新任务卡。
- 2026-08-12：修复两个“AI 工作岛”可同时运行。实测 PID 3456 来自项目 `dist/AI 工作岛.app`，PID 96254 来自 `/Applications/AI 工作岛.app`；仅注销 Launch Services 不足以阻止用户再次双击 dist。构建脚本现在将签名后的 dist 副本归档为 `dist/AI 工作岛.app.zip` 后删除可启动的 `.app`，只保留 `/Applications` 运行副本；debug 模式例外。
- 2026-08-12：完成 macOS 27 原生分层 App Icon。Icon Composer 2.0 在 Design Generation 27 下回读 5 图层/3 分组；首次预览发现图层顺序导致状态点被遮挡，重排为“状态点 → 工作岛 → 玻璃外壳”后，Default、Dark、TintedDark 三种官方 `ictool` rendition 均清晰。安装包同时包含校验一致的 `AppIcon.icon` 与回退 `AppIcon.icns`，Info.plist 同时声明 `CFBundleIconName`/`CFBundleIconFile`；110 项测试、严格签名、进程和自动化诊断通过。当前系统为 macOS 26.6，动态环境响应需在 macOS 27 升级后再做最终系统级回读。
- 2026-08-12：GitHub README 收敛为产品定位、核心能力、安装、自动化接入、隐私边界与已知限制，删除重复的实现细节枚举。新增 `docs/media/ai-work-island-demo.gif`，由真实安装版胶囊和面板回读生成，不包含 Token、Cookie、录音或转写稿。

- 2026-08-12：App Icon 按 macOS 27 Liquid Glass 语言重绘，保留横向工作岛与蓝/青/琥珀三枚状态点，将厚重金属外壳收敛为统一的烟灰透明玻璃容器，增加克制的镜面高光、折射与内层深度。1024px PNG 母版和全尺寸 ICNS 已更新并安装。
- 2026-08-12：会话输入新增剪贴板图片支持，覆盖新建会话与卡片定向续接；App Server 和桌面 IPC 均发送 `localImage` 输入，输入框无文字时 Delete/退格逐张移除图片，临时 PNG 在删除或请求结束后清理。110 项 Swift 测试通过，其中包含真实 `NSPasteboard` 图片、`Command-V` 与 Delete 事件回归；安装版辅助功能回读确认新任务与卡片输入框均正常显示。
- 2026-08-12：会议纪要转写状态接入现有自动化卡片协议，按 4 阶段展示“转写 → 生成纪要 → 写入 Obsidian → 推送排队”，并区分监听中、已转写待生成、完成与异常。真实服务重启后发布 `idle / 会议纪要监听中 / 暂无待处理录音`，安装版 `--diagnose-automation` 读回成功。
- 2026-08-12：修复显示器布局或面板高度变化后迷你胶囊可能继承越过屏幕上沿的旧锚点。每次进入收起态都会按当前屏幕 `visibleFrame` 将胶囊可见本体约束回屏幕内，再反向同步主面板锚点。108 项 Swift 测试、构建安装、严格签名和 `git diff --check` 通过；安装版 CoreGraphics 回读胶囊为 `(1320, 30, 120, 50)`、主面板为 `(1010, 30, 430, 342)` 且主面板 `onscreen=1`。
- 2026-08-12：桌面面板默认显示方式改为始终置顶浮窗；未保存过显示偏好的新安装会直接使用 `.floating`，菜单仍可手动切换为“置于普通窗口后方”。107 项 Swift 测试、构建安装、严格签名、进程和自动化诊断通过；本机偏好已写为 `backgroundPanelMode=0`，CoreGraphics 回读主面板与胶囊均为浮窗层级 3。
- 2026-08-12：完成录音守护 V1。新增语音备忘录录音状态胶囊、连续静音 5 分钟提醒、菜单与提醒中的“结束并保留 / 继续录音”，启动时申请必要的辅助功能权限；麦克风仅用于实时分贝判断。107 项 Swift 测试、构建安装、严格签名、进程和 `git diff --check` 通过。麦克风与辅助功能授权后完成真实闭环：`Option-Command-R` 创建录音，胶囊显示红色 `录音 0分`，从工作岛菜单执行“结束当前录音并保留”后 Capture 清空、数据库回读 52.3 秒及实体 m4a，胶囊恢复蓝色任务轮播。系统通知在当前 ad-hoc 安装下未获授权，静音提醒会回退为应用警告框；5 分钟静音阈值由确定性单元测试覆盖，未额外等待 5 分钟制造测试录音。
- 2026-08-12：按用户选择的 B 方案，将主面板统一为深色烟灰玻璃。新增集中式 `DesktopPanelPalette`，主背景、任务卡和输入框采用三档深灰层级，固定深色外观防止浅色系统下文字失去对比，蓝色继续只表达运行和聚焦。105 项 Swift 测试、`git diff --check`、构建安装、严格签名、进程及 Hermes/自动化诊断通过；安装版 555×303pt 截图回读确认主面板、任务卡、输入框与胶囊视觉一致，文字和状态清晰。
- 2026-08-12：桌面面板改为可调整尺寸。启用原生窗口四边/四角拖动，定义 320×260pt 至 620×746pt 范围；拖动结束后持久化用户尺寸，后续任务刷新不再自动覆盖，悬停收起和再次展开继续共用右上角锚点。104 项 Swift 测试、`git diff --check`、构建安装、严格签名和进程检查通过；安装版用真实鼠标将面板从 372×370pt 拖到 432×370pt，重启后先显示 120×50pt 胶囊，悬停恢复 432×370pt，移开后再次回到 120×50pt，截图确认任务卡与输入框随宽度伸展。
- 2026-08-12：迷你胶囊新增剩余额度轮播。4 秒依次显示任务状态、可用的 5 小时/周/公司额度，缺失项跳过，异常、失联和等待确认保持置顶；每秒快照刷新会保留当前轮播页。103 项 Swift 测试、`git diff --check`、构建安装、严格签名、进程、Hermes/自动化诊断通过；安装版辅助功能连续回读真实 `当前2运行 → 周额度剩余70% → 当前2运行`，当前 5 小时与公司额度无数据，未生成占位值。
- 2026-08-12：收敛会话卡片宽度。任务列表和每个分组/卡片显式约束为面板内容区全宽，静止态悬浮操作按钮退出布局，释放标题和项目路径空间；外框仍固定 372pt。100 项 Swift 测试、`git diff --check`、构建安装、严格签名、进程、Hermes/自动化诊断通过；安装版截图回读确认卡片左右留白对称、标题完整且输入框同步变宽。
- 2026-08-12：重新设计 App 与菜单栏图标。App 图标采用银色连续圆角外壳、黑色 Touch Bar 横带和中央三状态工作岛，1024px 母版带透明边缘；菜单栏版收敛为“细横带 + 中央鼓起岛”的 18pt 单色模板图标。新增 ICNS 生成脚本和菜单栏像素/模板回归测试。99 项 Swift 测试、`git diff --check`、构建安装、严格签名、进程、Hermes/自动化诊断通过；安装包 ICNS 与源码 SHA-256 一致，真实菜单栏截图回读确认轮廓和三枚状态孔清晰。
- 2026-08-12：项目元数据从 `CodexTouchBar` 收敛为 `AIWorkIsland`，用户可见名称仍为“AI 工作岛”。桌面面板移除标题栏 `＋`，改为圆角面板内的常驻“新任务”输入框；回车或发送可直接创建会话，文件夹按钮只用于切换并记住工作目录。兼容 Bundle ID、可执行文件名和自动化状态目录未变。97 项 Swift 测试、`git diff --check`、构建安装、严格签名及安装版辅助功能/截图回读通过；本机没有开发者证书，稳定辅助功能身份检查按预期报告 ad-hoc 签名。
- 2026-08-12：常驻“新任务”输入栏使用独立 HUD 材质容器，13pt 连续圆角、半透明玻璃层、亮边和聚焦时的系统强调色描边/柔光；原生文本框取消直角背景。输入时会阻止悬停自动收起，失焦后恢复既有收起策略。97 项 Swift 测试、`git diff --check`、构建安装和安装版展开/聚焦截图回读通过。
- 2026-08-12：任务卡内的“继续该项目”输入栏复用同一圆角玻璃组件；原生直角背景已移除，聚焦时同步显示强调色描边与柔光。97 项 Swift 测试、`git diff --check`、构建安装与任务卡输入栏展开/聚焦截图回读通过。
- 2026-08-12：修复输入框获得焦点后永久阻止悬停收起的问题。自动收起仅由鼠标是否离开面板决定；输入框焦点只影响其视觉态，不再改变面板收起状态机。97 项 Swift 测试、`git diff --check`、构建安装、严格签名与运行进程检查通过。
- 2026-08-11：修复胶囊与展开面板位置脱节及面板不能拖动。主面板/胶囊以共享右上角锚点双向同步，展开前由胶囊定位主面板，收起与主面板移动、尺寸变化时反向定位胶囊；背景空白区显式调用 AppKit 原生拖动。新增锚点往返回归测试；97 项 Swift 测试、`git diff --check`、构建安装、严格签名、运行进程和真实自动化诊断通过。
- 2026-08-11：主面板和迷你胶囊新增屏幕边缘吸附。窗口移动后按覆盖面积选择目标屏幕，以其 `visibleFrame` 的四边为准；18pt 内贴边，胶囊以可见本体而非 120×50pt 透明画布对齐。新增纯规则回归测试覆盖主面板双边及胶囊透明边距；96 项 Swift 测试、`git diff --check`、构建安装、严格签名、运行进程和真实自动化诊断通过。
- 2026-08-11：修复全屏应用内悬停胶囊后面板消失。背景模式下，胶囊展开期间主面板临时使用 `.floating` 层级与 `.fullScreenAuxiliary`，收起完成后恢复桌面图标层级；减少动态效果路径同步切换，旧缩小动画 completion 不会覆盖重新展开状态。94 项 Swift 测试、构建安装、严格签名和进程通过；真实 Edge 全屏窗口内悬停胶囊后，窗口从 120×50pt 展开为 372×290pt，截图确认完整面板位于全屏页面上方，测试后 Edge 已恢复非全屏状态。
- 2026-08-11：完成应用精简。移除 WidgetKit 扩展、Widget 快照/URL/诊断/构建签名链路；桌面面板标题栏改为按需新建会话，删除常驻新会话输入卡和面板录音入口；公司额度无数据时隐藏，额度区改为轻量文本；任务卡整卡点击负责打开任务，悬停只保留产出和详情；页脚只在状态文件异常时出现；菜单移除立即刷新、打开 Codex、打开 Hermes，并将窗口后方模式改名为“置于普通窗口后方”。93 项 Swift 测试、脚本语法、`git diff --check`、构建安装、严格签名、进程、登录项和真实自动化诊断通过；安装包内及 plug-in 注册表均无 Widget；安装版截图回读确认面板高度由原约 425pt 收至 290pt、公司额度无数据时不占位、标题栏仅保留状态与新建按钮。
- 2026-08-11：浮窗对话改为卡片定向续接。每张 Codex 项目/会话卡片内显示“上轮”短结果和“继续该项目”输入框，发送严格绑定卡片 thread ID；顶部移除“当前任务”选择，只保留“新建会话”，选择项目后才启用首条指令输入。`RolloutTailReader` 仅接受 final answer、过滤 commentary，去除 Markdown 与 `<oai-mem-citation>` 后限制 120 字；结果只进入内存展示，Widget 快照继续不含对话正文。97 项 Swift 测试、构建安装、严格签名和进程启动通过；安装版辅助功能树和截图回读确认 372pt 固定宽度、卡片内结果/输入/发送入口以及项目目录选择器，未向真实任务发送测试指令。
- 2026-08-11：应用正式更名为“AI 工作岛”；应用包、菜单退出项、桌面面板、悬浮胶囊辅助功能标签、Widget 图库名、URL 类型显示名、App Server 客户端标题、README 与验证脚本统一改名。Bundle ID、URL Scheme、二进制名和自动化数据目录作为兼容性标识保留。96 项 Swift 测试、构建安装、严格签名、进程、登录项、Widget 注册和自动化诊断通过；安装版辅助功能树与截图回读应用名、窗口标题和胶囊标签均为“AI 工作岛”。旧名称的安装包和 `dist` 构建包已移动到废纸篓，可恢复。
- 2026-08-11：桌面浮窗新增“当前任务 / 新建会话”与指令输入区。默认续接最近更新的运行任务；新建时选择项目目录，首条指令创建会话，后续指令通过同一 thread 继续。App Server 端到端实测 `thread/start → turn/start → completed` 返回 `OK`，再以 `thread/resume → turn/start` 返回 `RESUMED`；桌面运行任务回读确认独立 App Server 会被 `active writer` 拒绝，因此正式实现改走桌面 IPC thread-follower 通道。96 项 Swift 测试、构建安装、严格签名、进程、自动化/Widget 诊断和 `git diff --check` 通过；实机辅助功能树确认两个入口、输入框与发送按钮可用，输入框聚焦时保持展开，移开后恢复 108×38pt 胶囊。
- 2026-08-11：应用启动时桌面状态面板默认缩回为悬浮胶囊；胶囊首次显示于当前鼠标所在屏幕的右上角，之后保持用户可拖动位置。
- 2026-08-11：移除桌面面板标题栏内的“缩小为悬浮按钮”入口，标题栏只保留状态与录音操作。
- 2026-08-11：悬浮胶囊交互调整为临时悬停展开：指向胶囊即展开，离开主面板后延迟 0.5 秒自动缩回；返回面板会取消缩回。减少动态效果下沿用相同状态机、仅跳过过渡动画。95 项 Swift 测试覆盖离开/停留判断。
- 2026-08-11：修复“缩小后消失”：悬停恢复可在缩小动画结束前触发，旧 completion 此前仍会将已经恢复的主面板隐藏，造成两个窗口都不可见。现在 completion 只在仍处于缩小状态时隐藏面板，并有回归测试覆盖该竞态。
- 2026-08-11：桌面面板切换为 Liquid Glass 风格：主容器使用半透明系统材质、双层透光渐变、22pt 连续圆角、亮边与柔和阴影；额度、状态和任务卡使用独立玻璃层，任务卡悬停增加强调色反射；迷你悬浮胶囊改为带高光反射的深色玻璃。93 项 Swift 测试、`git diff --check`、严格签名和安装版进程启动通过；全屏截图回读确认可读性与玻璃层次。
- 2026-08-11：悬浮胶囊新增鼠标进入即恢复桌面面板；悬停与点击复用同一恢复路径，拖拽逻辑保留。93 项 Swift 测试、`git diff --check`、安装版严格签名、进程启动与自动化诊断均通过。
- 2026-08-11：TowerIsland 本机定制版更新到 1.3.4 build 1342。新增 `CodexQuotaMonitor`，每 15 秒只读扫描最近 Codex rollout 尾部的 `token_count.rate_limits`，收起刘海显示 5 小时/周额度中剩余更少的一项，展开标题栏显示两项；50% 以下橙色、20% 以下红色，无数据回退“已就绪”。鼠标悬停展开不再要求存在可见 Agent 会话，空闲刘海也会展开，移出沿用 0.5 秒保护收起。安装版回读真实 `周 80%`，严格签名、进程和 100 项 TowerIsland 集成测试通过。
- 2026-08-11：0.5.15 build 47 修复运行态沿边流光和悬停缩放的动画截断。沿边动画改为固定胶囊描边遮罩内旋转的正方形锥形渐变，渐变首尾均为透明，不再使用跨闭合路径起点的 `strokeStart/strokeEnd`；悬浮窗口增加每边 6pt 透明安全区，可见胶囊仍为 108×38pt。安装版连续抓取 15 帧覆盖完整 1.8 秒流光周期，截图稳定为 120×50px，关键转角帧无右侧硬断、内部斜切线或窗口裁边。92 项 Swift 测试、`git diff --check`、严格签名、0.5.15 build 47、进程和自动化诊断通过。
- 2026-08-11：0.5.14 build 46 完成悬浮胶囊分层动效增强。运行态同时挂载状态点呼吸、扩散环、背景漂移和沿边流光；状态切换同时挂载文字推入、状态点弹跳、色环扩散、胶囊弹性、状态色掠光和边框闪光；面板缩小/恢复增加连续窗口过渡。92 项 Swift 测试通过，其中动画层断言确认 9 个关键 Core Animation 动画已安装。安装版运行态 4 帧以 320ms 间隔采样，连续帧平均绝对像素差为 3.522、4.314、4.624，证明持续动效真实渲染；临时 waiting 状态回读为橙色“1 等待你”后已删除，状态目录只保留真实 `mario-daily.json`。`git diff --check`、严格签名、0.5.14 build 46、进程、面板缩小/恢复和自动化诊断均通过。
- 2026-08-11：修复 TowerIsland 新建 Codex 会话仍显示 `0 active`。根因不是 socket 或刷新，而是 TowerIsland 新增的 6 个生命周期钩子处于 Codex “需要审核”状态：原 Petdex 钩子每类 1 个已启用，TowerIsland 的第二个钩子及 `SessionStart` 均未信任，因而完全不执行。逐项确认命令仅调用本机 `~/.tower-island/bin/di-bridge` 后，通过 Codex 原生钩子审核界面选择 `Trust all and continue`，`config.toml` 已生成对应 6 条 `trusted_hash`。随后运行最小只读 Codex 测试会话，TowerIsland 日志真实收到 `session_start` 与 `status_update`，安装版展开界面回读到提示词、项目 `touchbar`、Codex 来源及回复 `OK`。既有会话不补录历史事件，后续提交可正常捕获。
- 2026-08-11：0.5.13 build 45 移除悬浮胶囊恢复后的“打开最近完成任务”回调。根因是最近完成的自动化任务只有 `outputPath`，通用任务打开逻辑因此调用 `NSWorkspace.open` 自动唤起 Finder。修复后胶囊点击只恢复桌面面板，辅助功能标签同步改为“恢复 AI 工作状态”；任务卡上的“打开产出”仍保留。安装版实机在 Finder 窗口数为 0 时完成“缩小→点击胶囊→恢复面板”，操作后 Finder 窗口仍为 0、前台应用仍为 Obsidian，没有自动打开或抢占焦点。91 项 Swift 测试、`git diff --check`、严格签名、安装版本、进程和自动化诊断通过。
- 2026-08-11：从 TowerIsland v1.3.4 固定源码本地构建并重新 ad-hoc 签名后安装；发布 DMG 的 SHA-256 与 GitHub 摘要一致，但原包严格签名校验失败，因此未直接使用。首次启动只增加 6 个 Codex hooks，去除 TowerIsland 条目后与备份完全一致，未写入 Claude、Cursor、Gemini、OpenCode 等配置。仓库 100 项 bridge/会话/权限/问题/多 Agent 集成测试全部通过；Swift 单元测试因当前 Command Line Tools 缺少 `XCTest` 模块未运行。安装副本版本、严格签名、进程和 socket 通过，测试模拟会话已清理，实机回读恢复黑色 “Ready” 胶囊。
- 2026-08-11：0.5.12 build 44 参考 TowerIsland 的克制节奏增强迷你灵动岛动效：运行状态点同步透明度与缩放呼吸；状态变化触发一次文字淡入、胶囊轻弹和状态色掠光；悬停轻微放大，系统开启“减少动态效果”时关闭持续和状态切换动效。安装副本实机回读为黑色胶囊、蓝色状态点和“2 运行”，辅助功能入口为“恢复并打开最近完成：下载数据并合并；当前2 运行”，点击后主面板正常恢复。91 项 Swift 测试、`git diff --check`、严格签名、安装版本、进程状态和自动化诊断通过。
- 2026-08-11：0.5.11 build 43 将悬浮按钮改为 108×38pt 黑色迷你灵动岛胶囊；状态优先级为异常、等待、运行、完成、空闲，使用红、橙、蓝、绿、灰状态点和简短文字，运行状态支持遵守“减少动态效果”的呼吸动画。安装副本实机回读为黑色胶囊、蓝色状态点和“1 运行”，辅助功能标签保留最近完成任务入口，点击后主面板正常恢复。90 项 Swift 测试、`git diff --check`、严格签名、安装版本、进程状态和自动化诊断通过。
- 2026-08-11：0.5.10 build 42 将 58×58pt 悬浮按钮外轮廓改为 29pt 半径正圆，保留原生材质、细描边、状态圆盘和交互行为。安装副本实机截图确认外轮廓为完整圆形，辅助功能标签仍为“打开最近完成：下载数据并合并”，点击后主面板正常恢复。90 项 Swift 测试、`git diff --check`、严格签名、安装版本、进程状态和自动化诊断通过。
- 2026-08-11：0.5.9 build 41 将悬浮按钮改为 58×58pt 的 macOS 原生轻量样式：系统 popover 材质、18pt 连续圆角、细描边、低对比状态圆盘、SF Symbol 及悬停/按压反馈；完成和空闲状态使用不同系统图标与辅助功能标签。安装副本实机截图回读为浅色圆角材质、淡绿色圆盘和细线勾号，辅助功能标签为“打开最近完成：下载数据并合并”，点击后主面板正常恢复。90 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。
- 2026-08-11：0.5.8 build 40 新增桌面面板缩小悬浮按钮与最近完成任务入口。悬浮按钮可拖动，完成任务存在时使用绿色完成图标；点击恢复面板并打开最新可访问的已完成任务。实机回读悬浮按钮为“打开最近完成：下载数据并合并”，点击后面板恢复且 Finder 打开真实产出目录“思维底表”。89 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。
- 2026-08-11：README 面向中文用户统一标题和功能说明，保留命令、路径、协议字段及必要技术专名原文。
- 2026-08-11：0.5.7 build 39 取消 Touch Bar 推理程度滑块及其菜单入口、待应用状态和后台写入链路；主界面只保留任务状态、Codex 额度和公司额度。87 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。
- 2026-08-11：0.5.6 build 38 将无归属项目的分组名称统一为中文“任务”。已安装窗口实机回读两条运行任务均显示“正在处理 · 任务 · 执行中”；87 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。
- 2026-08-11：0.5.5 build 37 使用 Codex 桌面端本机 IPC 设置目标会话的推理程度，移除无限等待的辅助功能重试主链路。实机在当前任务执行中完成 `low → medium → low`，每次 IPC 均返回成功且 `state_5.sqlite` 同步回读；当前会话最终为 `low`，历史待应用状态已清除。87 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。
- 2026-08-11：0.5.4 build 36 将推理选择绑定到目标任务并持久保存。任务执行中显示“低·待”等明确状态；任务结束或应用重启后继续自动应用，目标任务数据库回读一致后才清除待应用状态。当前用户选择已登记为本任务 `low`，等待本轮完成后应用。85 项 Swift 测试与 `git diff --check` 通过。
- 2026-08-11：0.5.3 build 35 将 `等待你` 分组改为固定置顶、始终展开、不可折叠，等待任务逐项显示并优先占用全部 7 个可见位置。推理滑块改为优先直接操作 Codex 推理菜单并支持中文标签；实测旧快捷键在当前 Codex 任务执行中会返回假成功但数据库仍为 `medium`，新实现此时保留待确认选择并在任务结束后自动重试，数据库回读一致才确认。85 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。实机临时注入 waiting 任务后回读到置顶的禁用折叠标题与独立任务行；测试文件已删除，状态目录仅剩真实 `mario-daily.json`。当前任务执行中诊断诚实返回“未找到推理程度控件”，数据库保持 `medium`，未误改档位。
- 2026-08-11：0.5.2 build 34 在桌面状态窗口增加 Codex 与公司额度摘要；Codex 自动显示服务端实际提供的 5 小时/周窗口，公司额度显示剩余美元和百分比。修复每秒轮询删除并重建全部任务行及同级运行任务按日志时间反复换位导致的闪烁，改为可见内容签名去重、活动任务稳定排序、额度原位更新；页脚使用任务真实更新时间，不再每秒跳动。84 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态通过。实机窗口回读到 `Codex 额度 周 94%` 与 `公司额度 暂不可用`，连续 5 帧跨越 4 次轮询时任务顺序、文字和页脚完全一致，无列表淡出。
- 2026-08-11：0.5.1 build 33 增加完整剩余额度显示。Codex 胶囊同时支持 5 小时和周窗口，以两者较低值驱动警示圆环；公司胶囊直接显示剩余美元金额。安装后诊断回读为周额度剩余 94%，当前数据源未提供 5 小时窗口；公司额度因 Edge 未保留已登录平台标签页而不可用。82 项 Swift 测试、`git diff --check`、严格签名、安装版本和进程状态均通过，桌面状态面板实机回读正常。
- 2026-08-10：0.5.0 build 32 完成桌面看板五项增强：顶部状态改为会随工作态切色和弹性更新的胶囊；任务完成及异常恢复分别使用绿色、蓝色掠光；悬浮任务卡显示“打开任务 / 打开产出 / 详情”快捷操作；自动化协议新增 `phase`、`phaseIndex`、`phaseCount` 并渲染分段进度轨道；任务按“正在执行 / 等待查询 / 需要处理 / 最近完成”自动分组和折叠，首次展开最重要的一组。结构动画保留顶边锚定并遵守系统“减少动态效果”。实机回读状态胶囊、分组、悬浮按钮与 4/5 阶段轨道，折叠时窗口由 280pt 平滑收至 218pt；临时状态文件已删除。81 项 Swift 测试、`git diff --check`、严格签名和安装版本 32 均通过。
- 2026-08-10：0.5.0 build 31 为桌面任务看板加入结构变化动效：仅在可见任务 ID/顺序变化时触发，先用 0.10 秒淡出旧列表，再以 0.34 秒窗口顶边锚定缩放、淡入和纵向弹簧归位呈现新列表；连续刷新在动画结束后合并处理，避免每秒状态轮询打断动画。系统开启“减少动态效果”时完全跳过大幅动画，直接更新布局。实机注入临时任务回测，窗口增长高度连续采样为 360→366→374→387→394→405→411→418→422，缩短也连续回落至 298；临时状态文件已清理。81 项 Swift 测试通过。
- 2026-08-10：0.5.0 build 30 新增 `queued/等待查询`，用于系统排队或等待外部查询；`waiting/等待你` 仅表示确实需要人工处理。排队状态按活动任务频率刷新，但不计入“待处理”。Mario 权限问题处理后的当前状态已改为 `queued`。
- 2026-08-10：WidgetKit 原生“AI 工作状态”中号/大号小组件已嵌入 0.5.0 build 29。刷新策略为面板可见 1 秒、后台运行中 3 秒、后台空闲 30 秒；状态、步骤或产出变化才申请重载，Timeline 按运行 5 分钟、待处理 10 分钟、空闲 30 分钟自适应。运行快照超过 2 分钟显示“数据延迟”、超过 10 分钟显示“状态未更新”，读取失败不误报为空闲。桌面面板改为中性自适应玻璃背景和低对比卡片；`WorkItem.displayTitle/displayDetail` 清理附件名、Markdown 标题和换行，所有标题/步骤严格单行截断，避免溢出和窗口变宽。异常或失联任务单击后先显示问题详情，不再自动打开文件夹；弹窗默认操作为“关闭”，有链接或产出时提供次级“打开详情/打开产出”。Codex 任务标题优先读取 `~/.codex/session_index.jsonl` 的最新 `thread_name`，覆盖 `state_5.sqlite` 中可能滞后的初始提问标题，使看板和会话标题同步。任务点击通过 Codex 原生深链 `codex://threads/{thread-id}` 按稳定 ID 跳转，不再依赖标题匹配或辅助功能树。每次冷启动均默认展示桌面任务看板；手动关闭只影响当前运行周期。任务点击曾通过私有搜索快捷键粘贴标题并回车；搜索未打开时会把标题发进当前会话，build 25 已彻底移除该输入链路及搜索快捷键。实际窗口截图已回读验证背景、卡片和文字层级；曾尝试移除整个标题栏以消除灰色窗口按钮，但会使 AppKit 重建 contentView、主体消失，已回退并保留稳定布局。小组件、桌面面板、菜单栏及全局快捷键 `Option-Command-R` 调用同一录音流程。build 16/17 曾因 `NSWorkspace` completion 违反 `@MainActor` 导致 `SIGTRAP`，build 18 起已修复。扩展已启用，80 项 Swift 测试通过。禁止自动展开系统组件图库。
- 2026-08-10：修复桌面面板被长标题/详情撑宽；宽度固定为 372pt，仅高度随可见任务数变化，标题和详情均单行截断，浮窗也取消缩放按钮。新增宽度回归测试，74 项 Swift 测试通过；0.3.0 build 12 已安装。实机注入超长标题后窗口仍为 372pt，截图确认省略号正确，临时任务已删除。
- 2026-08-10：桌面面板增加小组件模式，默认位于与 macOS 原生桌面组件相同的窗口层级、低于普通应用窗口；交通灯按钮已隐藏，圆角、移动和点击能力保留。实机窗口截图和 CoreGraphics 层级均已回读。
- 2026-08-10：首个真实自动化“下载数据并合并”已接入；桌面小组件回读到 `Mario 数据自动化 / 下载数据并合并 / 空闲`，当前真实文案为等待下次 09:00 运行。
- 2026-08-10：新增统一状态中枢、桌面状态面板和自动化 JSON 协议；73 项 Swift 测试通过。0.3.0（build 10）已安装到 `/Applications` 并以新进程运行；实机回读确认 Codex 与临时自动化任务可同时显示，临时状态文件已移除，自动化诊断回读为 0 项且无异常。
- 2026-08-07：完全访问会话实机回归 `low → xhigh → low` 均成功，数据库逐次回读一致，`approval_mode=never`、`sandbox_policy={"type":"disabled"}` 全程未变化；根因是快捷键队列丢步，不是权限限制。
- 2026-08-07：推理滑块改为从当前会话数据库状态回读，68 项测试通过；实机写入回归被 `CodexRestartRequired` 正确阻止，未改动当前会话的 `low`，需用户在本轮完成后重启一次 Codex 再验证写入。
- 2026-08-07：从菜单实际触发“重启应用”，已验证旧进程退出且 `/Applications` 安装副本以新 PID 重新启动。
- 2026-08-07：67 项 Swift 测试通过；Info.plist、快捷键 JSON、安装包严格签名、单实例重开、Codex/Hermes/公司额度/登录项诊断均通过。
- “搜索聊天”实测可切换后返回当前会话；本轮未出现来自 Codex UI 的 `Conversation state not found`，日志中的同文案仅来自回归命令文本本身。
- 辅助功能树可读取；设置 Electron 增强模式返回 `-25205`，当前搜索跳转不依赖该设置，暂作为系统兼容性告警保留。

## 未解决

- 当前结果卡目标只有结束本轮后才会进入完成态，无法在同一轮取得该卡的完成态截图；完整正文读取、完成态无限行、真实宽度测高和留白约束均已有回归覆盖。安装版旧兜底卡片问题已闭环：旧进程实际从 09:58 持续运行，源码更新未进入可见进程；发布闸门重装并重启为新 PID 后，`--ui-review-panel` 首帧“正在读取…”在 3 秒内自动替换为真实线程标题、项目名和公开正文，不再停在纯省略号或旧卡片。
- 新版排版已通过 `--ui-review-panel` 安装版回读，hover 防循环和边缘展开/回缩坐标状态机已有回归测试；Computer Use 仍无法可靠命中无边框胶囊的真实拖拽，需用真实鼠标补验“拖到边缘 → 展开 → 回缩”的最终观感，以及完成摘要胶囊的 6 秒观感。
- Appium Mac2 不再用于验收，因为会触发 `testmanagerd` 第三方输入法授权；多屏、跨重启任务和精确会话跳转已由双屏环境、真实进程/线程状态及回归测试完成验收。
- 当前机器是 macOS 26.6；升级到 macOS 27 后需回读 Dock/Finder 在背景与外观变化下对原生 `AppIcon.icon` 的动态高光、折射和半透明渲染。
- 浮窗对话只展示当前轮最近 4 条公开说明、最新一条工具动作、计划进度和最近回复，不承载隐藏推理或完整多轮消息记录；完整上下文仍以 Codex 会话为准。
- Codex 额度展示服务端窗口的剩余百分比，不是逐线程精确 Token 数；若账号只返回周窗口，5 小时额度不显示。
- 公司额度实时刷新仍依赖 Edge 保留已登录的平台标签页；标签休眠和应用重启已有持久化缓存与后台唤醒重试，平台退出登录后只能展示最后一次成功值。
- 后续自动化接入若要显示真实阶段进度，需在每个阶段更新 `phase`、`phaseIndex`、`phaseCount`；缺失时看板会正常退化为无轨道卡片。
- 正式 Developer ID 签名和 notarization。
- 私有 Touch Bar API 的系统升级兼容性。
- 其他自动化程序仍需逐个接入状态 JSON；首个 Mario“下载数据并合并”已完成接入。
- TowerIsland 仓库未提供 LICENSE 文件，当前只作为本机试用和视觉参考；不要把其源码复制进本项目，除非作者补充许可证或明确授权。

## 验证命令

```bash
swift test --disable-sandbox --scratch-path /tmp/codex-hermes-touch-bar-tests
./script/build_and_run.sh --verify
/Applications/AI工作岛.app/Contents/MacOS/CodexTouchBar --diagnose-hermes
/Applications/AI工作岛.app/Contents/MacOS/CodexTouchBar --diagnose-automation
/Applications/AI工作岛.app/Contents/MacOS/CodexTouchBar --diagnose-effort low
```
